# manual-agent 流式输出打印器执行文档

## 1. 文档状态

本文档是 `manual-agent` 流式输出打印器的执行方案。

- 当前状态：已完成实现，Maven 编译和测试通过；尚未进行真实模型接口联调。
- 执行目标：把模型流式响应解析为少量明确的事件类型，再由打印器根据事件类型选择展示策略。
- 本次范围：thinking、普通文本、工具调用、工具结果、消息结束、原始信息落盘。
- 本次不处理：换行策略、其他协议兼容、服务端工具、未知内容块、真实接口联调。

## 2. 已确认的约束

| 项目 | 确认内容 |
| --- | --- |
| thinking | 在模型请求中启用 thinking，预算固定为 8192；模型返回的 thinking 内容增量完整打印，并保留完整内容块 |
| 打印事件 | 使用 `ParsedEventType` 区分打印策略 |
| 工具调用 | 工具调用完整结束后再打印，按模型返回顺序打印 JSON |
| 工具结果 | 按调用顺序打印；终端最多显示 2000 字符 |
| 大工具结果 | 超过 2000 字符时，完整结果写入文件，终端显示前 2000 字符和文件路径 |
| 原始信息 | 原始模型流式事件额外写入 JSONL；本地生成的工具调用完成记录和工具结果也写入追踪文件 |
| 父 Agent | 继续向当前终端输出 |
| 子 Agent | 继续保持终端静默，但完整原始结果和大工具结果仍然落盘 |
| 测试 | 不要求用户进行真实模型测试；实现后只做必要的编译和静态检查 |

## 3. 当前代码基线

### 3.1 模型文本输出入口

当前父 Agent 在以下位置通过 `Consumer<String>` 接收模型文本：

`src/main/java/dev/learn/agent/manual/ManualAgentApplication.java`

```java
agentLoop.run(
        history,
        recalledMemories,
        text -> {
            System.out.print(text);
            System.out.flush();
        }
);
```

该接口只能表达“普通文本字符串”，无法表达以下信息：

- thinking 增量；
- 工具调用已经完整结束；
- 工具调用对应的工具结果；
- message stop；
- 原始模型事件。

### 3.2 当前流式解析入口

`AgentLoop.createStreamingTurn(...)` 当前直接消费 `RawMessageStreamEvent`，已经支持：

- `text` 内容块；
- `tool_use` 内容块；
- `input_json_delta` 工具参数增量；
- `message_delta` 的 `MAX_TOKENS` 判断；
- `message_stop` 结束判断。

当前遇到 thinking 内容块或 thinking 增量时会进入不支持分支，因此需要在同一条流式解析链中补齐 thinking。

### 3.3 当前工具结果入口

`AgentLoop.collectToolResults(...)` 已经按照 `PendingToolExecution` 的列表顺序等待 Future，因此它天然提供了工具结果的展示顺序约束：

```text
模型返回工具调用 A、B
工具 A、B 可以并发执行
collectToolResults 仍然按 A、B 顺序 join 和交付
```

打印器应当在每次 `join()` 取得原始 `ToolExecutionResult` 后立即接收 `TOOL_RESULT` 事件，再交给现有 `ContextManager` 做模型上下文预算处理。

这样终端和落盘拿到的是完整原始结果，而不是已经被上下文预算替换过的预览结果。

## 4. 目标架构

```text
RawMessageStreamEvent
        │
        ├── 原始事件落盘
        │     └── StreamOutputPrinter.recordRawEvent(rawEvent)
        │
        └── AgentLoop 解析
              ├── TEXT_DELTA
              ├── THINKING_DELTA
              ├── TOOL_CALL_COMPLETED
              ├── MESSAGE_STOPPED
              └── 工具执行完成后生成 TOOL_RESULT
                         │
                         ▼
                 StreamOutputPrinter.print(parsedEvent)
                         │
                         ├── 终端展示
                         └── 完整结果落盘
```

本方案明确分离两类输入：

1. `RawMessageStreamEvent` 是 SDK 原始协议事件，用于完整追踪和解析。
2. `ParsedEvent` 是应用内部已经识别出的打印事件，用于选择终端展示策略。

打印策略层不接收无类型的 `Object`、`Map` 或预先拼好的 JSON 字符串。`ParsedEvent` 中的业务载荷仍然使用明确的 Java 类型，例如 `ToolUseBlock` 和 `ToolExecutionResult`。

## 5. 事件模型

### 5.1 事件类型

新增枚举：

```java
public enum ParsedEventType {
    TEXT_DELTA,
    THINKING_DELTA,
    TOOL_CALL_COMPLETED,
    TOOL_RESULT,
    MESSAGE_STOPPED
}
```

本次只实现这五类事件，不增加协议兼容分支。

### 5.2 ParsedEvent 结构

建议新增一个不可变事件记录：

```java
public record ParsedEvent(
        ParsedEventType type,
        String text,
        ToolUseBlock toolUse,
        ToolExecutionResult toolResult
) {
    public static ParsedEvent textDelta(String text) {
        return new ParsedEvent(
                ParsedEventType.TEXT_DELTA,
                text,
                null,
                null
        );
    }

    public static ParsedEvent thinkingDelta(String text) {
        return new ParsedEvent(
                ParsedEventType.THINKING_DELTA,
                text,
                null,
                null
        );
    }

    public static ParsedEvent toolCallCompleted(
            ToolUseBlock toolUse
    ) {
        return new ParsedEvent(
                ParsedEventType.TOOL_CALL_COMPLETED,
                null,
                toolUse,
                null
        );
    }

    public static ParsedEvent toolResult(
            ToolUseBlock toolUse,
            ToolExecutionResult toolResult
    ) {
        return new ParsedEvent(
                ParsedEventType.TOOL_RESULT,
                null,
                toolUse,
                toolResult
        );
    }

    public static ParsedEvent messageStopped() {
        return new ParsedEvent(
                ParsedEventType.MESSAGE_STOPPED,
                null,
                null,
                null
        );
    }
}
```

字段契约如下：

| 事件类型 | `text` | `toolUse` | `toolResult` |
| --- | --- | --- | --- |
| `TEXT_DELTA` | 必填 | 不使用 | 不使用 |
| `THINKING_DELTA` | 必填 | 不使用 | 不使用 |
| `TOOL_CALL_COMPLETED` | 不使用 | 必填 | 不使用 |
| `TOOL_RESULT` | 不使用 | 必填 | 必填 |
| `MESSAGE_STOPPED` | 不使用 | 不使用 | 不使用 |

工厂方法负责构造正确的字段组合，打印器不需要猜测某个字段是否存在。

## 6. 原始事件和 ParsedEvent 的生成顺序

### 6.1 普通文本

收到 `content_block_delta` 且 delta 类型为文本时：

```text
1. 把原始 RawMessageStreamEvent 写入 JSONL。
2. 从 delta 取出文本。
3. 把文本追加到当前文本块。
4. 创建 ParsedEvent.textDelta(text)。
5. 交给 StreamOutputPrinter.print(...).
```

文本必须在收到增量后立即生成 ParsedEvent，不等到 `content_block_stop`。

### 6.2 thinking

收到 thinking 内容块时，必须维护独立的 thinking 状态：

```text
当前内容块类型：thinking
当前 thinking 文本：StringBuilder
当前 thinking signature：StringBuilder
```

收到 thinking 增量时：

```text
1. 把原始事件写入 JSONL。
2. 取出 thinking delta 文本。
3. 追加到当前 thinking 文本。
4. 创建 ParsedEvent.thinkingDelta(text)。
5. 交给打印器完整打印。
```

signature 只用于组装完整的 thinking 内容块，不转换成 `THINKING_DELTA`，也不直接展示到终端。

收到 `content_block_stop` 时：

```text
1. 使用完整 thinking 文本和 signature 构造 ThinkingBlockParam。
2. 转换为 ContentBlockParam。
3. 按模型返回顺序加入 assistantContent。
4. 清理当前内容块状态。
```

thinking 内容不能只打印后丢弃。它必须进入 `assistantContent`，因为工具调用后的下一轮模型请求需要保留完整的 assistant 内容块顺序和 thinking 签名。

thinking 内容不加入 `completedText`。`StreamTurn.text()` 仍然只表示普通模型文本结论，避免子 Agent 将 thinking 当成最终工具结果返回给父 Agent。

### 6.3 工具调用完成

工具调用参数由多个 `input_json_delta` 拼接而成。拼接期间不创建 ParsedEvent，不向终端输出半截 JSON。

收到 `content_block_stop` 后：

```text
1. 解析完整工具 JSON。
2. 使用解析后的 input 重建 ToolUseBlock。
3. 先创建 TOOL_CALL_COMPLETED 事件并交给打印器。
4. 再提交工具执行任务。
5. 把 ToolUseBlock 和 Future 按模型顺序保存到 toolExecutions。
```

“先打印工具调用，再提交执行”是为了保证终端顺序稳定。即使工具执行很快完成，也不能让工具结果先于工具调用显示。

工具调用打印内容应包含：

- 工具名称；
- tool use ID；
- 完整 input JSON。

JSON 使用现有 SDK 的 JSON 映射器序列化，不手工拼接字段。

### 6.4 工具结果

在 `collectToolResults(...)` 中，每个 Future 按现有列表顺序 `join()` 后：

```text
1. 得到原始 ToolExecutionResult。
2. 创建 ParsedEvent.toolResult(toolUse, result)。
3. 交给 StreamOutputPrinter。
4. 再构造 ToolResultBlockParam。
5. 最后统一调用 ContextManager.applyToolResultBudget(...).
```

打印器接收的必须是未经 `ContextManager` 替换的完整结果。

### 6.5 message stop

收到 `message_stop` 时：

```text
1. 把原始事件写入 JSONL。
2. 设置 AgentLoop 的 messageStopped 状态。
3. 创建 ParsedEvent.messageStopped()。
4. 交给打印器记录消息结束并执行结束策略。
```

`MESSAGE_STOPPED` 不携带文本或工具数据，主要用于记录当前模型请求已经正常结束；请求文件由 `AgentLoop` 的 `finally` 统一关闭。

## 7. StreamOutputPrinter 执行策略

### 7.1 统一入口

打印器提供统一分发入口：

```java
public void print(ParsedEvent event) {
    switch (event.type()) {
        case TEXT_DELTA -> printText(event.text());
        case THINKING_DELTA -> printThinking(event.text());
        case TOOL_CALL_COMPLETED -> printToolCall(event.toolUse());
        case TOOL_RESULT -> printToolResult(
                event.toolUse(),
                event.toolResult()
        );
        case MESSAGE_STOPPED -> printMessageStopped();
    }
}
```

打印器只根据 `ParsedEventType` 选择策略，不再根据字符串内容猜测当前输出是什么。

### 7.2 文本策略

`printText(String text)` 的职责只有两件事：

1. 将本次文本增量写入父 Agent 的终端；
2. 立即刷新终端输出。

本阶段不处理换行归一化，因此文本中的换行原样交给终端。

子 Agent 使用相同策略对象，但终端输出目标为空输出流，因此不会显示到父终端。

### 7.3 thinking 策略

`printThinking(String text)` 与普通文本一样，完整打印每个 thinking 增量，不设置 2000 字限制。

thinking 的 2000 字限制不适用，因为用户明确要求 thinking 内容块完整打印。完整 thinking 同时存在于：

- 终端输出，父 Agent 可见；
- 原始 JSONL 文件；
- assistant 历史中的 `ThinkingBlockParam`。

### 7.4 工具调用策略

`printToolCall(ToolUseBlock toolUse)` 输出完整工具调用 JSON。

建议输出结构：

```text
工具调用：<tool name>
{
  "id": "...",
  "name": "...",
  "input": {
    ...
  }
}
```

工具调用不做字符截断，因为它通常是模型控制流程的关键证据，而且用户已经确认完整 JSON 可以直接阅读。

### 7.5 工具结果策略

`printToolResult(ToolUseBlock toolUse, ToolExecutionResult result)` 按以下规则执行：

| 结果长度 | 终端 | 文件 |
| --- | --- | --- |
| `<= 2000` 字符 | 完整输出 | 不额外创建工具结果文本文件，但会写入原始追踪 JSONL |
| `> 2000` 字符 | 输出前 2000 字符 | 写入完整 UTF-8 文本文件 |

终端必须明确说明是否发生截断，例如：

```text
工具结果：read_file
结果长度：12842 字符，终端显示前 2000 字符
完整结果：.task_outputs/stream-outputs/.../tool-result-xxxx.txt

<前 2000 字符>
```

文件中保存未经截断的 `result.content()`。`result.error()` 同时写入文件头和 JSONL，避免把工具失败结果误认为普通成功结果。

## 8. 原始信息落盘

### 8.1 文件粒度

每一次真实模型请求生成一个 JSONL 文件，而不是每次用户输入只生成一个文件。

原因是一次 `AgentLoop.run(...)` 可能包含多次模型轮次和输出续写。按真实模型请求拆分后，原始流和 `message_stop` 的边界清晰。

建议目录结构：

```text
.task_outputs/
└── stream-outputs/
    └── <session-id>/
        ├── parent/
        │   ├── request-<request-id>.jsonl
        │   └── tool-result-<file-id>.txt
        └── subagent/
            ├── request-<request-id>.jsonl
            └── tool-result-<file-id>.txt
```

目录名使用程序生成的 UUID，不使用模型生成的工具名称、路径或参数作为文件名。

### 8.2 JSONL 记录类型

原始追踪文件至少包含以下记录：

```json
{"record_type":"model_event","agent":"parent","event":{...}}
{"record_type":"tool_call_completed","agent":"parent","tool_use":{...}}
{"record_type":"tool_result","agent":"parent","tool_use":{...},"result":{...}}
```

其中：

- `model_event.event` 是 SDK 原始事件 JSON；
- `tool_call_completed` 是应用根据多个原始事件重建的完整工具调用；
- `tool_result` 是本地工具执行产生的结果，因为它不是模型 SSE 事件，必须额外记录。

原始事件每收到一条就写入文件，不能等整个请求结束后才统一写入。这样模型流中断时，已经收到的 thinking、文本和工具参数仍然可追踪。

### 8.3 大工具结果文件

大工具结果文件使用 UTF-8 普通文本，内容保持原始工具结果，不进行 JSON 转义或截断。

文件头只添加必要元信息：

```text
工具名称：read_file
工具调用 ID：toolu_xxx
是否报错：否
原始结果字符数：12842

==================== 完整工具结果 ====================

<完整 result.content()>
```

文件路径通过现有 `WorkspacePathResolver` 生成，保持工作区写入边界，不允许模型输入直接决定物理路径。

## 9. thinking 请求和消息历史改动

### 9.1 请求参数

在 `AgentLoop.createRequest(...)` 中增加固定 thinking 配置：

```java
private static final long THINKING_BUDGET_TOKENS = 8_192L;
```

请求构造器使用 SDK 已有的 thinking enabled 配置，预算为 `THINKING_BUDGET_TOKENS`。

本次不增加：

- adaptive thinking；
- 多模型配置；
- 运行时命令行开关；
- 其他服务商兼容字段。

### 9.2 assistant 历史

正常结束时，`assistantContent` 的顺序必须与模型返回顺序完全一致：

```text
thinking block
text block
tool_use block
text block
```

其中 thinking block 通过 SDK 的 `ThinkingBlockParam` 加入 `ContentBlockParam`。

中断时仍沿用当前规则：只有收到 `content_block_stop` 的完整块才进入历史；已经打印但尚未结束的 thinking 块不进入下一轮消息。不过，未完成内容的原始 delta 已经存在于 JSONL 文件中。

## 10. AgentLoop 接口改动

### 10.1 删除 Consumer<String> 输出边界

当前方法：

```java
run(messages, turnContext, Consumer<String> textOutput)
```

改为：

```java
run(messages, turnContext)
```

`AgentLoop` 持有一个构造时注入的 `StreamOutputPrinter`。

这样可以避免旧文本回调和新事件打印器同时存在，防止普通文本重复输出或换行逻辑分裂。

### 10.2 需要移除回调参数的位置

以下方法不再接收 `Consumer<String>`：

- `AgentLoop.run(...)`；
- `AgentLoop.requestModel(...)`；
- `AgentLoop.createStreamingTurn(...)`；
- `AgentLoop.interruptedTurn(...)`。

原有的普通文本回调调用改为创建 `ParsedEvent.textDelta(...)`；中断、轮次耗尽和输出截断提示通过 `StreamOutputPrinter.printLocalMessage(...)` 输出，不新增 ParsedEvent 类型，也不进入模型消息历史。

### 10.3 构造顺序

在 `ManualAgentApplication` 中：

```text
1. 创建父 Agent 的 StreamOutputPrinter。
2. 创建子 Agent 的静默 StreamOutputPrinter。
3. 创建 subagentLoop，并注入子 Agent 打印器。
4. 创建 parentLoop，并注入父 Agent 打印器。
5. TaskTool 继续持有 subagentLoop。
```

子 Agent 的终端静默不是通过 `ignored -> {}` 回调实现，而是通过打印器的终端输出目标实现；原始文件输出保持开启。

## 11. 文件修改清单

### 11.1 新增文件

| 文件 | 职责 |
| --- | --- |
| `src/main/java/dev/learn/agent/manual/output/ParsedEventType.java` | 定义五类解析事件 |
| `src/main/java/dev/learn/agent/manual/output/ParsedEvent.java` | 携带事件类型和强类型载荷 |
| `src/main/java/dev/learn/agent/manual/output/StreamOutputPrinter.java` | 事件分发、终端输出、原始 JSONL 和大工具结果落盘 |

### 11.2 修改文件

| 文件 | 修改内容 |
| --- | --- |
| `AgentLoop.java` | 注入打印器、启用 thinking、解析 thinking、生成 ParsedEvent、打印工具结果 |
| `ManualAgentApplication.java` | 创建父子打印器并传入 AgentLoop，删除文本 Consumer |
| `TaskTool.java` | 适配新的 `subagentLoop.run(messages, turnContext)` 签名，保持终端静默语义 |

### 11.3 不修改文件

`ContextManager.java` 本次不修改其上下文预算逻辑。

它继续负责：

- 发送给模型的工具结果总预算；
- 模型上下文中的旧工具结果压缩；
- 已压缩结果的模型可读文件引用。

打印器的终端 2000 字符限制和 `ContextManager` 的 2000 字符预览是两个不同边界，不能合并。

## 12. 执行顺序

### 阶段一：建立事件模型

- 新增 `ParsedEventType`；
- 新增 `ParsedEvent`；
- 为五类事件提供静态工厂方法；
- 不接入 AgentLoop。

完成标志：事件类型和字段契约可以独立编译。

### 阶段二：建立打印器

- 新增父 Agent 和子 Agent 的终端输出配置；
- 实现 `print(ParsedEvent)` 的 switch；
- 实现普通文本和 thinking 的完整输出；
- 实现工具调用 JSON 输出；
- 实现工具结果 2000 字符截断；
- 实现 JSONL 和完整工具结果文件落盘。

完成标志：打印器可以接收构造好的 ParsedEvent，不依赖模型网络请求。

### 阶段三：接入 thinking 请求

- 在 `createRequest(...)` 启用 thinking；
- 使用 8192 预算；
- 在当前块状态中增加 thinking 文本和 signature；
- 在块关闭时生成 `ThinkingBlockParam`。

完成标志：thinking 能进入 `assistantContent`，不会被当作最终文本返回。

### 阶段四：接入流式解析

- 每个原始事件先落盘；
- 普通文本 delta 转换为 `TEXT_DELTA`；
- thinking delta 转换为 `THINKING_DELTA`；
- 完整工具调用转换为 `TOOL_CALL_COMPLETED`；
- message stop 转换为 `MESSAGE_STOPPED`；
- 保持现有工具执行和中断恢复边界。

完成标志：AgentLoop 不再依赖 `Consumer<String>`，但普通文本、thinking、工具调用和消息历史均保持正确。

### 阶段五：接入工具结果

- 在 `collectToolResults(...)` 中按现有顺序生成 `TOOL_RESULT`；
- 打印原始结果；
- 大结果写入完整文本文件；
- 再交给 `ContextManager` 应用模型上下文预算。

完成标志：终端看到的工具结果不会被上下文压缩逻辑替换，父子 Agent 都能保存完整结果。

### 阶段六：修改应用装配

- 创建 parent printer；
- 创建静默 subagent printer；
- 调整两个 `AgentLoop` 构造调用；
- 调整 `TaskTool` 的 run 调用；
- 删除旧的文本回调和无条件尾部换行。

完成标志：主 Agent 终端可见，子 Agent 终端静默，双方文件均有原始追踪记录。

## 13. 验收标准

| 编号 | 验收条件 |
| --- | --- |
| 1 | 请求参数包含 thinking enabled 和 8192 预算 |
| 2 | `RawMessageStreamEvent` 每收到一条就有对应 JSONL 记录 |
| 3 | 普通文本 delta 立即生成 `TEXT_DELTA` |
| 4 | thinking delta 完整生成 `THINKING_DELTA`，没有 2000 字截断 |
| 5 | thinking 内容块关闭后进入 assistant 历史，并保留 signature |
| 6 | 工具 JSON 未完整关闭前不会打印半截调用 |
| 7 | 工具调用先于对应工具结果打印 |
| 8 | 工具结果按照模型调用顺序打印 |
| 9 | 大工具结果终端只显示前 2000 字符，完整内容在文件中 |
| 10 | `ContextManager` 仍然只负责模型上下文预算，不负责终端显示 |
| 11 | 子 Agent 不向主终端打印模型中间内容，但有完整落盘文件 |
| 12 | `AgentLoop` 不再使用 `Consumer<String>` 传递模型输出 |
| 13 | 正常完成时产生 `MESSAGE_STOPPED` |
| 14 | 不引入本次范围外的协议兼容分支 |

## 14. 本次明确不做的事情

### 14.1 不处理换行策略

本阶段只保证文本和 thinking 增量按到达顺序输出，不对标题、空行、重复换行做统一规范化。

换行问题单独作为后续任务处理，避免把事件解析正确性和终端排版问题混在一起。

### 14.2 不处理其他协议类型

本阶段不增加以下内容：

- redacted thinking；
- server tool；
- web search result；
- citation delta；
- 未知内容块自动降级；
- 其他厂商的字段兼容。

这些事件如果当前 SDK 解析到但没有对应分支，沿用现有明确失败行为，不静默丢弃。

### 14.3 不改变工具执行并发模型

工具仍由现有 `ToolCallDispatcher` 调度；本次只改变工具调用和工具结果的观察方式，不重写工具执行线程模型。

## 15. 最终实现边界

```text
AgentLoop
    负责：协议解析、thinking 组装、工具调度、消息历史

StreamOutputPrinter
    负责：ParsedEvent 分发、终端输出、原始追踪、结果落盘

ContextManager
    负责：发给模型的上下文预算和压缩

TaskTool
    负责：子 Agent 调用和最终结论返回
```

这个边界保证打印逻辑不会改变 Agent 的业务执行语义，也保证完整输出不会被模型上下文压缩逻辑意外截断。

## 16. 实施后验证方式

用户不需要先进行真实模型测试。本次实现完成后，只做以下验证：

1. 检查新增类型和现有 SDK 类型的编译关系；
2. 执行 `manual-agent` 的 Maven 编译；
3. 静态确认两个 AgentLoop 的打印器装配关系；
4. 静态确认工具结果打印发生在 `ContextManager.applyToolResultBudget(...)` 之前；
5. 静态确认子 Agent 使用静默终端但没有关闭文件落盘。

真实接口返回的 thinking 事件形态和 DashScope 实际输出，可以在后续需要时单独验证，不作为本次执行文档的前置条件。
