# manual-agent 流式终端输出说明

## 1. 当前目标

本模块只解决 Agent 运行时的可读终端输出：

- thinking 内容完整流式打印；
- Agent 正文完整流式打印；
- 完整工具调用以 JSON 打印；
- 工具结果最多在终端显示前 2000 字符；
- 父 Agent 显示中间过程，子 Agent 保持终端静默。

本模块不负责原始事件归档、请求 Trace 或人工查看 2000 字符之后的工具结果。

## 2. 三类数据边界

```text
模型原始流事件
        │
        ▼
AgentLoop
  ├─ 解析 thinking、文本和工具调用
  ├─ 组装可回传的 assistant 内容
  └─ 调用 StreamOutputPrinter
             │
             └─ 终端展示

工具结果 ──► ContextManager
             └─ 只在上下文预算超限时保存完整结果
```

职责边界如下：

| 组件 | 负责内容 |
| --- | --- |
| `AgentLoop` | 协议解析、thinking/signature 组装、工具调度和消息历史 |
| `StreamOutputPrinter` | 终端标题、流式文本、工具 JSON 和工具结果预览 |
| `ContextManager` | 模型上下文预算、必要的完整工具结果落盘 |

原始 SSE 事件不写入终端输出目录。未来若需要回放、评测或审计，应另行设计 Trace 组件。

## 3. 输出接口

`StreamOutputPrinter` 只依赖一个 `PrintStream`：

```java
public void startThinkingBlock();
public void printThinkingDelta(String text);
public void startTextBlock();
public void printTextDelta(String text);
public void printToolCall(ToolUseBlock toolUse);
public void printToolResult(
        ToolUseBlock toolUse,
        ToolExecutionResult result
);
public void printLocalMessage(String message);
```

不再使用通用 `ParsedEvent`。当前只有一个终端消费者，直接方法可以让调用点表达真实业务含义，也避免可空联合字段和事件分发状态。

## 4. 模型流处理顺序

### 4.1 thinking

收到 thinking 内容块开始事件时，AgentLoop：

1. 保存起始 thinking 文本和 signature；
2. 调用 `startThinkingBlock()`；
3. 对每个 thinking 增量调用 `printThinkingDelta(...)`；
4. 在内容块结束时使用完整文本和 signature 构造 `ThinkingBlockParam`；
5. 将 thinking 块按模型顺序加入 assistant 历史。

signature 只用于协议回传，不打印到终端。使用 thinking 和工具调用时，完整 thinking 块必须保留，不能只保存可读文本。

### 4.2 普通文本

收到文本内容块开始事件时调用 `startTextBlock()`；每个文本增量立即调用 `printTextDelta(...)`。文本不会等到整个模型响应结束才输出。

### 4.3 工具调用

工具参数可能由多个 `input_json_delta` 组成。AgentLoop 只在内容块结束、JSON 已完整拼接并重建 `ToolUseBlock` 后调用 `printToolCall(...)`，不会把半截 JSON 打到终端。

工具调用展示使用 SDK JSON 映射器序列化，不手工拼接字段。

### 4.4 工具结果

`collectToolResults(...)` 按模型返回顺序等待 Future。拿到完整 `ToolExecutionResult` 后先调用 `printToolResult(...)`，再交给 `ContextManager.applyToolResultBudget(...)`。

终端行为：

| 结果长度 | 终端行为 | 文件行为 |
| --- | --- | --- |
| 不超过 2000 字符 | 完整显示 | 不由输出器创建文件 |
| 超过 2000 字符 | 显示前 2000 字符并提示截断 | 不由输出器创建文件 |

输出器只截取终端预览，不修改传给模型的原始结果。

### 4.5 message stop

`message_stop` 仍由 AgentLoop 用于判断模型流是否完整结束，但不再生成可见的“消息结束”文本。模型可能刚结束工具调用请求，后面仍要执行工具，因此这个标记不代表整个任务结束。

## 5. 上下文结果落盘

`ContextManager` 保留原有的上下文预算治理：同一轮工具结果总字符数超过本地预算时，选择结果写入 `.task_outputs/tool-results`，再把文件路径和短预览交给模型。

这套落盘的消费方是模型上下文，不是终端用户。它不能被本次终端输出收缩一并删除。

## 6. 父子 Agent 输出

父 Agent 使用 `System.out` 创建打印器：

```java
new StreamOutputPrinter(System.out)
```

子 Agent 使用空输出流：

```java
new StreamOutputPrinter(
        StreamOutputPrinter.silentTerminal()
)
```

两者共享同一套输出行为，但子 Agent 的 thinking、正文、工具调用和工具结果不会进入父终端；最终结论仍通过 `task` 工具返回。

## 7. 测试与验收

测试覆盖：

1. thinking 和正文多次增量完整输出；
2. thinking 与正文具有可读边界；
3. 工具调用包含完整 JSON 输入；
4. 短工具结果完整输出；
5. 长工具结果只输出前 2000 字符并提示截断；
6. 截断不会切断 UTF-16 代理对；
7. 子 Agent 静默终端不抛出异常；
8. `ContextManager` 原有上下文落盘测试继续通过。

验证命令：

```text
mvn test
git diff --check
```

## 8. 明确不做

- 不保存原始模型 SSE 事件；
- 不创建请求级 JSONL 文件；
- 不为每个大工具结果创建额外文本归档；
- 不添加 Trace、回放、评测或审计接口；
- 不修改既有 `ContextManager` 上下文预算规则；
- 不增加其他供应商或其他协议内容块的兼容分支。
