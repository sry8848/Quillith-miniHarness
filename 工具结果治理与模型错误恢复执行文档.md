# GitHub MCP 工具结果治理与模型请求错误恢复执行文档

## 1. 范围与最终边界

本次保留已有的工具结果治理能力，并只整理主模型请求级的内容拒绝恢复。

保留的既有能力：

- McpAgentTool 继续透传模型生成的工具参数，不由宿主补写参数。
- GitHub 仓库搜索工具合同继续提供查询限定、分页和精简结果提示。
- ContextManager 继续负责中段裁剪、旧 Tool Result 压缩、主动历史摘要和超大结果落盘。
- MCP CallToolResult.isError=true 继续通过同一个 tool_use_id 生成失败 tool_result，交给模型自行修正。
- 普通网络断流和流式 partial turn 继续使用现有 SSE/中断处理路径。

本次新增的模型请求级恢复：

- 入口是 AgentLoop 发起真实模型请求时抛出的 AnthropicServiceException。
- 当前只注册 ContentRejectionRecoveryHandler，只识别 Provider Body 根字段 code=DataInspectionFailed。
- Handler 只处理最近一批尚未被后续模型请求接受的 Tool Result。
- 可恢复时，Handler 直接修改消息历史并返回 true；AgentLoop 在同一主循环中重试。
- 没有 pending Tool Result、批次无法完整定位、次数耗尽或异常未匹配时返回 false，由 AgentLoop 原样抛出异常。
- Prompt Too Long 不新增 Provider fallback Handler。请求前已有主动压缩；Provider 仍拒绝时按未知/不可恢复异常抛出。
- SSE Provider Error 不进入 ModelRequestRecoveryManager，不与 partial turn 恢复合并成同一状态机。

本次不扩大到子 Agent 协议、TaskTool、AgentResult、CLI 历史协议或通用 fallback 系统。

## 2. 唯一主循环和请求级恢复流程

    AgentLoop 外层业务轮次
        ↓
    请求前 Hook 与现有上下文治理
        ↓
    while 当前轮次仍需处理
        ↓
    createStreamingTurn(createRequest(...))
        ├─ AnthropicServiceException
        │      ↓
        │  recoveryManager.tryRecover(exception, state)
        │      ├─ true  → Handler 已修改历史 → continue
        │      └─ false → throw exception
        │
        ├─ StreamTurn.providerError()
        │      → 现有 SSE Provider Error 处理
        │
        └─ 正常响应
               → 丢弃旧恢复 State，创建新的 State
               → 提交 assistant/tool_result
               → 进入下一业务轮次

AgentLoop 只负责发起请求、调用 Manager、执行 continue 或抛出原异常，以及在请求成功后创建新 State。它不判断具体 Provider 错误类型、恢复次数或具体消息替换规则。

## 3. 新增生产类型

新增目录：

    src/main/java/dev/learn/agent/manual/recovery/
    ├─ ModelRequestRecoveryState.java
    ├─ ModelRequestRecoveryHandler.java
    ├─ ContentRejectionRecoveryHandler.java
    └─ ModelRequestRecoveryManager.java

### 3.1 ModelRequestRecoveryState

只保存当前连续请求错误恢复链需要的最小状态：

- AgentLoop 当前维护的可修改消息历史；
- Content Rejection 已完成的恢复次数；
- 最近一批尚未被后续模型请求接受的 Tool Result ID。

一次模型请求成功后，AgentLoop 直接用同一份 messages 创建新的 State，不提供也不调用旧对象清零方法。Manager 无运行状态，父 Agent 和子 Agent 可以共享 Manager；每次 run 都创建自己的 State。

### 3.2 ModelRequestRecoveryHandler

接口只有两个方法：

    boolean supports(AnthropicServiceException exception);

    boolean tryRecover(
            AnthropicServiceException exception,
            ModelRequestRecoveryState state
    );

supports 负责错误归属；tryRecover 负责该错误的恢复条件、次数限制和消息/状态修改。Handler 不调用模型、不执行工具、不实现 Agent 循环。

### 3.3 ModelRequestRecoveryManager

Manager 保存按优先级排列的 Handler 列表，遍历并把异常交给第一个 supports 返回 true 的 Handler。第一个匹配 Handler 返回 false 时立即结束，不继续调用后续 Handler。没有 Handler 支持时返回 false。Manager 不识别具体 Provider 错误。

## 4. ContentRejectionRecoveryHandler 策略

### 4.1 错误识别

只在 Provider 返回可解析 JSON，且 Body 根字段精确为 code=DataInspectionFailed 时接管。普通 HTTP 400、相似错误消息、嵌套字段或无法解析的 Body 不自动归入该 Handler。

### 4.2 恢复条件和消息修改

tryRecover 按以下顺序执行：

1. 已使用两次恢复机会时返回 false，不修改历史；
2. 没有 pending Tool Result 时返回 false，不重试用户第一次输入或 Hook 上下文；
3. 从历史尾部寻找最近一条 user block message；
4. 只有 pending ID 全部位于该消息中时才创建副本并一次性替换；
5. 任一 ID 缺失时返回 false，不提交半批修改。

每个命中的 tool_result 只做以下修改：

- 保留原 tool_use_id；
- 保留 assistant tool_use、工具名称和工具输入；
- 保留 tool_result block 和同一 user 消息中的其他 block；
- 将结果正文替换为固定的安全交付失败说明；
- 设置 is_error=true；
- 修改成功后才递增恢复次数并返回 true。

工具已经真实执行，恢复不会删除 tool_use，也不会尝试回滚外部副作用。原始结果不再进入下一次模型请求。

## 5. AgentLoop 的具体接入点

### 5.1 依赖注入

AgentLoop 增加 ModelRequestRecoveryManager 构造参数。ManualAgentApplication 创建只含 ContentRejectionRecoveryHandler 的 Manager，并注入父、子两个 AgentLoop。

### 5.2 请求调用点

删除只转发参数的一次性请求包装，主循环直接调用 createStreamingTurn(createRequest(...))。模型请求异常只执行：

    tryRecover(exception, requestRecoveryState)
        true  → continue
        false → throw exception

这里不添加针对 Prompt Too Long、DataInspectionFailed、HTTP 状态码或次数的分支。

### 5.3 pending Tool Result 记录

正常 Tool Result 成对提交后、流中断提交完整 Tool Result 后，以及现有 SSE Provider Error 提交工具事实后，记录本批 tool_use_id。下一次模型请求被 Provider 接受后，旧 State 立即废弃，下一批错误从新 State 计数。

### 5.4 异常边界

- SseException 仍在 createStreamingTurn 内转换为 StreamTurn.providerError，由现有 recoverFromProviderError 处理。
- AnthropicIoException 和 AnthropicInvalidDataException 仍按现有完整内容块提交和中断续接流程处理。
- CLI 仍在 ManualAgentApplication 的 run 边界输出 Provider 诊断并回滚失败回合。

## 6. ContextManager 范围

保留以下主动治理算法和阈值：

    snipMiddle
    compactOldToolResults
    shouldAutoCompact
    compactHistory
    compactMessages
    summarizeHistory
    applyToolResultBudget

删除没有生产调用点的 Prompt Too Long 异常恢复入口及其定位辅助方法。主动压缩仍在请求前执行；本次不增加 Tokenizer、不重新估算 Provider Token、不调整现有字符阈值。

## 7. 测试与验收证据

新增测试：

    src/test/java/dev/learn/agent/manual/recovery/
    ├─ ModelRequestRecoveryManagerTest.java
    ├─ ContentRejectionRecoveryHandlerTest.java
    └─ AgentLoopModelRequestRecoveryIntegrationTest.java

策略测试验证：

- Manager 按注册顺序选择第一个匹配 Handler；
- 第一个匹配 Handler 返回 false 时不调用后续 Handler；
- 根 code 精确识别；
- pending 批次原子替换；
- 无 pending、批次不完整、次数耗尽时历史不变。

集成测试使用真实 AnthropicOkHttpClient、本地 HTTP Provider 和真实 AgentLoop，覆盖：

1. tool_use → DataInspectionFailed → 安全 tool_result → 成功文本，工具只执行一次，恢复请求保留 tool_use_id 和工具事实且不包含原始结果；
2. 没有 pending Tool Result 时抛出原始 AnthropicServiceException，不多发请求；
3. 连续拒绝达到上限后抛出原始异常，不多发恢复请求；
4. 一次请求成功后形成新 pending 批次，证明新 State 不继承旧恢复次数。

本地 HTTP 服务验证 SDK、AgentLoop 和 Handler 的真实调用契约，不等同于线上百炼内容护栏的实网验收。

已执行并通过：

    mvn -q clean test
    git diff --check

旧请求包装、Prompt Too Long fallback、旧恢复入口和旧恢复常量已检索不到残留引用。

## 8. 最终验收标准

- AgentLoop 仍是唯一推进 Agent 执行状态的主循环；Handler 不调用模型、不执行工具。
- 请求级异常只有 tryRecover → continue / 原异常抛出一条路径。
- Manager 只遍历有序 Handler 列表，不包含具体错误判断。
- Content Handler 独立完成错误识别、次数限制、消息替换和 pending 状态处理。
- 工具执行事实和 tool_use_id 不因恢复被删除；不可安全定位时不盲目重试。
- 正常请求成功后通过新建 State 结束上一条恢复链。
- Prompt Too Long、未知 Provider 错误和不可恢复 Content Rejection 不重试，直接抛出原异常。
- SSE/partial turn 保持现有独立处理，不进入请求级 Manager。
- 主动上下文压缩、Tool Result 预算、MCP isError、TaskTool、CLI 回滚和 AgentResult 范围未被本次恢复抽象扩大。
- 完整 Maven 测试、策略边界测试和真实 SDK/HTTP/AgentLoop 集成测试全部通过。

