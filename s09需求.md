# 一、先给 Agent Memory 一个工程上的定义

如果把 Agent 简化一下：

```
用户输入   ↓当前上下文   ↓LLM 推理   ↓工具调用 / 输出
```

它天然存在一个问题：

> **下一次运行时，模型本身并不会因为上一次运行发生过什么而永久改变。**

所以一个没有外部状态管理的 Agent，本质上很像：

```
一个每次调用都重新启动的函数
```

你给它什么上下文，它就知道什么。

不给它，它就“不知道”。

因此 Memory System 真正做的事情可以概括成：

```
过去发生了很多事情        ↓判断哪些值得保留        ↓组织、压缩、存储        ↓当前任务到来        ↓判断哪些过去信息现在有用        ↓取出来放进有限 Context        ↓LLM 基于“过去 + 现在”做决策
```

所以：

> **Agent Memory 本质上是一个长期状态管理 + 信息筛选 + 上下文构建系统。**

而不是一个单纯的数据库模块。

---

# 二、它首先对应的是软件工程里一个非常经典的问题：状态管理

假设你写一个面试 Agent。

第一轮：

```
用户：我目标岗位是 Java 后端，最近主要准备 Redis 和 JVM。
```

Agent 能回答得很好。

三天后用户新开一个会话：

```
用户：按照我最近的准备情况，再面我一次。
```

如果没有 Memory：

```
Agent：你最近准备了什么？
```

用户会立刻觉得：

> 这东西根本不认识我。

这里真正缺的并不是“大模型能力”。

而是：

```
userId = 123长期状态：target_job = Java 后端recent_topics = [Redis, JVM]weakness = ...interview_history = ...
```

这其实跟传统后端非常像：

```
HTTP 本身无状态↓ 所以我们发明SessionCookieDatabaseRedis
```

Agent 也面临同样的问题。

只不过传统 Web 系统保存的大多是：

```
userId购物车登录状态订单状态
```

Agent 需要保存的却是：

```
用户偏好历史经历任务进展过去犯过的错误过去总结出的策略某个项目当前状态
```

因此第一个真实工程问题就是：

# Agent 如何从“无状态调用”变成“有状态长期服务”？

OpenAI Agents SDK 目前甚至直接提供了 `Session` 抽象：每次运行前读取会话历史，运行之后再把新消息、工具调用等写回存储；同时又单独区分了用于“从过去运行中提炼经验”的 Agent Memory。

这个区分很重要，我们后面会反复用到。

---

# 三、但“保存所有历史”马上产生第二个问题：Context 不可能无限增长

最简单的 Memory 实现其实是：

```
List<Message> history;
```

每次：

```
System Prompt+ 所有历史聊天+ 当前问题→ LLM
```

短期完全可行。

但对长期 Agent 来说很快会崩。

假设一个用户用了半年：

```
第 1 次对话第 2 次对话...第 500 次对话
```

你不能每次请求都：

```
把半年所有记录发给模型
```

即使模型支持非常大的 context window，也依然存在：

### token 成本

历史越多：

```
input token ↑
```

每一次请求都需要重新处理这些历史。

OpenAI Agents SDK 文档也明确指出，Session 中以前的消息可能再次作为 input 输入，因此后续调用的输入 token 会随历史增加；SDK 也提供 history limit 和 compaction 来解决这个问题。

### 延迟

```
Context 越大     ↓prefill 越长     ↓响应延迟增加
```

### 信息干扰

还有一个比成本更麻烦的问题：

> **上下文更多，不一定意味着回答更好。**

例如用户现在问：

```
我 Redis 最近哪里比较薄弱？
```

500 次历史里真正相关的可能只有：

```
第 182 次 Redis 面试第 236 次 Redis 复盘第 417 次 Redis 面试
```

剩下：

```
JavaSpring旅游天气MySQL闲聊……
```

反而都是噪声。

LangGraph 的文档也直接把这个问题列为短期记忆管理的核心问题：长对话可能超出 context window，因此需要 trim、delete、summarize 或其他过滤策略。

于是 Memory 的第二个真实工程问题出现了：

# 不是“怎么把过去全部记住”，而是“有限 Context 下应该想起什么”。

这才真正开始进入 Agent Memory 的核心。

---

# 四、所以 Memory 本质上存在一个“海量过去 → 小量当前上下文”的漏斗

你可以先记住这个模型：

```
                所有历史              100000 条                  ↓           Memory System        ┌────────────────┐        │ 写入 / 压缩     │        │ 索引            │        │ 检索            │        │ 排序            │        │ 更新 / 遗忘     │        └────────────────┘                  ↓           当前相关记忆              5~20 条                  ↓             Context                  ↓                LLM
```

所以 Memory System 实际解决的是一个：

> **信息压缩与信息恢复问题。**

进一步拆就是两个最根本的问题：

```
过去发生事情时：What should I remember?什么值得记？当前要做事情时：What should I recall?现在应该想起什么？
```

几乎所有 Agent Memory 技术最终都绕不开这两个问题。

---

# 五、需求进一步升级：Agent 不只是要“记聊天”，还需要记不同类型的东西

这是很多初学 Memory 时容易混在一起的地方。

举个 coding agent。

今天 Agent 帮你排查了一个服务：

```
第一次：发现项目启动必须设置 JAVA_HOME。第二次：发现测试环境数据库不能直接 truncate。第三次：发现这个项目运行 integration test 前必须启动 Redis。
```

一个真正长期工作的 Agent，下一次应该知道：

```
这个项目有三个坑：1. JAVA_HOME2. 测试库不能 truncate3. integration test 依赖 Redis
```

注意：

这些已经不是简单的：

```
聊天记录
```

而是：

```
经验
```

OpenAI 当前的 Agent Memory 文档也专门把它与 conversation Session 区分：

```
Session→ 保存 conversation historyAgent memory→ 从过去运行中提炼 lessons
```

并明确提到记忆可以减少之后重复探索的成本，以及记住用户纠正和偏好。

LongMemEval-V2 进一步研究的是这种“经验型记忆”，包括：

```
环境静态状态动态状态变化工作流程环境中的坑前提条件
```

而不仅仅是“用户之前说过什么”。

所以从真实产品需求看，Memory 至少存在几种完全不同的需求。

---

# 六、真实 Agent 的五类核心记忆需求

## 1. 会话连续性

最基本。

```
User:我的项目是 Spring Boot。User:它为什么启动失败？Agent 必须知道：“它” = 前面的 Spring Boot 项目
```

解决：

> 当前任务的短期状态连续性。

通常称为：

```
short-term memorysession memorythread memory
```

LangGraph 就把短期记忆定义为 thread-scoped memory。

---

## 2. 跨会话用户记忆

例如：

```
用户目标：Java 后端用户水平：大二用户不喜欢直接给答案，希望先分析用户薄弱点：Redis cluster
```

关闭程序以后重新打开：

```
这些仍然存在
```

这是：

```
long-term user memory
```

目标是：

> 个性化。

---

## 3. 任务 / 世界状态记忆

例如一个长期 coding Agent：

```
当前 branch = feature/login已经修改 UserService测试失败在 LoginServiceTestPR #32 等待 review
```

这些不是“用户偏好”。

而是：

> 当前世界处于什么状态。

这个问题更接近：

```
workflow statecheckpointtask state
```

这就是为什么严格来说：

> **State 和 Memory 不能完全混为一谈。**

---

## 4. 经验记忆

例如 Agent 曾经犯错：

```
错误：直接修改 generated code后来用户纠正：这个目录永远不要手改
```

下一次 Agent 应该：

```
避免再次犯错
```

这类信息更像：

```
过去事件    ↓总结经验    ↓形成策略
```

这也是长期自主 Agent 真正有价值的一类 Memory。

---

## 5. 共享组织记忆

假设：

```
Agent A：代码 AgentAgent B：测试 AgentAgent C：Review Agent
```

公司规定：

```
禁止直接 push main必须运行 integration testcommit message 使用 Conventional Commits
```

三个 Agent 都应该知道。

这就变成：

```
organization memoryshared memory
```

Letta 当前就提供 shared memory block：多个 Agent 可以挂载同一个 memory block，一个 Agent 更新后其他 Agent也能看到。

这已经开始碰到：

> 分布式系统中的共享状态问题。

---

# 七、到这里，你会发现 Agent Memory 其实对应了很多传统工程问题

不是一个问题。

而是一组问题。

| Agent Memory 问题 | 对应传统工程问题                         |
| --------------- | -------------------------------- |
| 保存对话状态          | Session / State Management       |
| 长期持久化           | Database                         |
| 快速找到相关历史        | Search / Information Retrieval   |
| 找最相关内容          | Ranking                          |
| 历史压缩            | Data Compression / Summarization |
| 删除过时信息          | Cache Eviction / Data Lifecycle  |
| 修改错误记忆          | Consistency                      |
| 多 Agent 共享      | Distributed Shared State         |
| 用户之间隔离          | Multi-tenancy                    |
| 同时修改记忆          | Concurrency Control              |
| 敏感信息处理          | Security / Privacy               |
| 记忆是否有效          | Observability / Evaluation       |

所以如果以后有人说：

> “Agent Memory 不就是 Redis + Vector DB 吗？”

现在应该已经能看出来为什么这个理解很浅。

Vector DB 最多解决：

```
“给定 query，从大量信息里找语义相似内容”
```

但 Memory System 还需要解决：

```
什么应该写？写成什么？什么时候写？写到哪里？什么时候取？取哪些？历史发生冲突怎么办？过期怎么办？用户删除怎么办？多个 Agent 修改怎么办？取出来以后 Agent 有没有正确利用？
```

---

# 八、真正难的第一个痛点：到底什么东西值得记？

考虑用户一句：

```
我今天中午吃了麦当劳。
```

要不要存？

不知道。

如果 Agent 是：

```
健身营养 Agent
```

可能非常重要。

如果 Agent 是：

```
Java 编程 Agent
```

几乎完全没意义。

再比如：

```
用户：我不喜欢 Lombok。
```

这可能应该长期保存。

但：

```
用户：这个项目暂时不用 Lombok。
```

这可能只是：

```
project-scoped
```

而不是：

```
user-scoped
```

所以 Memory 写入不是：

```
save(message);
```

而是一个判断问题：

```
importancerelevancescopelifetimeconfidence
```

也就是：

> **Memory formation：什么信息应该成为记忆？**

这是第一大难点。

---

# 九、第二个痛点：应该怎么表示记忆？

最简单的是直接保存：

```
User: 我以后写 Java 不喜欢用 Lombok。
```

但以后检索可能更希望得到：

```
{  "type": "preference",  "subject": "java",  "key": "lombok",  "value": "avoid"
}
```

甚至：

```
UserPreference └ Java    └ Lombok       preference = avoid       confidence = 0.9       updatedAt = ...
```

这是一个非常典型的工程 trade-off：

```
Raw Conversation      ↓信息完整，但噪声多Summary      ↓信息紧凑，但可能丢细节Structured Fact      ↓容易查询，但抽取可能出错Embedding      ↓容易语义检索，但缺乏精确结构Graph      ↓关系表达强，但系统复杂
```

现实中往往不会只有一种。

Letta 当前其实就把不同规模/重要程度的信息分成了多层：

```
Memory blocks→ 始终进入 contextFiles→ 部分读取Archival memory→ 需要搜索External RAG→ 大规模外部数据
```

本质就是：

> 不是所有 Memory 都应该采用同一种存储和访问方式。

---

# 十、第三个痛点：该想起来的时候想不起来

例如已经存了：

```
用户之前说过：我的 Redis 最大薄弱点是集群故障转移。
```

现在用户问：

```
继续针对我薄弱的地方面试。
```

查询里根本没有：

```
Rediscluster故障转移
```

如果只做简单向量相似度：

```
query embedding        ↓vector search
```

很可能找不到。

这就是：

# Retrieval Problem

现实中的 memory query 往往：

```
当前问题+ 用户+ 当前任务+ 最近状态+ 时间+ Agent 当前意图
```

共同决定应该检索什么。

AutoGen 的 Memory 抽象也不是单纯 Storage，而专门提供：

```
addqueryupdate_context
```

也就是把：

```
存储检索把 Memory 放回模型上下文
```

明确拆开。

---

# 十一、第四个痛点：记忆会过时、冲突

这是生产环境特别重要的问题。

例如：

一月份：

```
用户：我的目标岗位是 Java 后端。
```

五月份：

```
用户：我现在准备转 Agent 开发。
```

Memory 里面不能简单存在：

```
目标岗位 = Java目标岗位 = Agent
```

然后随机 retrieval。

又例如：

```
2025:项目 Java 172026:升级 Java 21
```

Agent 下一次如果取到旧 Memory：

```
“项目使用 Java 17”
```

可能直接产生错误决策。

所以 Memory 不是 append-only 日志那么简单。

必须面对：

```
updatesupersedeinvalidateexpiremergeconflict resolutiontemporal reasoning
```

LongMemEval 专门把：

```
knowledge updatestemporal reasoning
```

作为长期记忆需要测试的核心能力。研究中甚至发现，长历史条件下现有系统的记忆表现会明显下降。

所以：

> **“记住错误的信息”很多时候比“忘记”更危险。**

---

# 十二、第五个痛点：记忆越来越多以后，Memory 自己也会变成垃圾场

长期 Agent 很可能运行：

```
1 年
```

产生：

```
10 万条 message3 万次 tool call1 万个任务数千条总结
```

如果什么都保存：

```
Memory↓越来越大↓越来越多重复↓越来越多冲突↓retrieval precision 下降
```

最终形成：

> Memory pollution。

于是还需要：

```
deduplicationconsolidationcompressionforgettingTTLimportance decay
```

也就是：

> Agent 不但要学会记，还必须学会忘。

这其实和人脑也很像：

```
所有经历 ≠ 所有经历永久保持最高优先级
```

---

# 十三、第六个痛点：找到的 Memory 不代表 Agent 真用对了

这点非常容易被忽略。

假设问题答案需要 Memory A。

系统：

```
成功存储 A       ✓成功检索 A       ✓成功放入 context ✓LLM 最后理解错了 ✗
```

最终任务还是失败。

因此一个 Memory 系统至少存在三个阶段：

```
Ingestion信息有没有正确形成 Memory？Retrieval需要的时候有没有取出来？UtilizationAgent 有没有正确利用？
```

最近针对专业工程场景的 IFCMemoryBench 就采用类似的拆分方式，并发现现有系统即使能检索到主题相关信息，也经常把项目知识保存成**不完整或碎片化事实**，最后仍然无法完成任务。

这告诉我们一个非常重要的工程认识：

> **Memory 检索准确率 ≠ Agent 最终能力。**

---

# 十四、第七个痛点：Memory 会进入安全边界

假设系统里：

```
user A memoryuser B memorycompany A memorycompany B memory
```

如果检索过滤错误：

```
user A   ↓retrieve   ↓user B 的历史
```

这已经不是模型“幻觉”。

而是：

> 数据泄露事故。

所以生产 Memory 必然涉及：

```
user_idtenant_idagent_idsession_idnamespaceACL
```

还包括：

```
删除用户数据TTL审计加密数据保留策略
```

OpenAI Agents SDK 当前提供的 Session backend 中甚至已经有：

```
EncryptedSessionTTLRedisSQLAlchemyMongoDBDapr
```

这说明一旦进入生产环境，Agent Memory 很快就不再只是 AI 问题，而变成标准的后端数据基础设施问题。

---

# 十五、因此我们可以把 Agent Memory 的“需求分析”正式总结成 7 个能力

如果以后真的做一个 Agent Memory 模块，我建议先不用写任何代码，先写下面这张需求表。

```
                 Agent Memory System1. Persist   过去的信息能不能留下？2. Select   什么值得成为 Memory？3. Represent   Memory 应该以什么形式保存？4. Retrieve   当前任务应该召回哪些 Memory？5. Update   信息改变以后怎么修改旧 Memory？6. Forget   什么 Memory 应该删除/降低优先级？7. Govern   谁能访问、修改、删除哪些 Memory？
```

而整个系统最终优化的目标只有一个：

```
          正确的 Memory               ↓        在正确的时间出现               ↓      占用尽可能少的 Context               ↓       帮助 Agent 做出更好决策
```

---

# 十六、现在尤其需要建立三个边界：Memory ≠ History ≠ RAG

这三个东西特别容易混。

## Conversation History

解决：

```
“刚才发生了什么？”
```

例如：

```
User：帮我分析这个 bug。Agent：把日志发给我。User：就是这个。
```

需要知道：

```
“这个”指前文
```

---

## Agent Memory

解决：

```
“过去有哪些东西会影响现在的决策？”
```

例如：

```
这个用户讨厌 Lombok这个项目不能修改 generated/上一次跑测试需要先启动 Redis用户之前在 JVM GC 问题上连续答错三次
```

---

## Knowledge Base / RAG

主要解决：

```
“外部世界有什么知识？”
```

例如：

```
Spring 官方文档公司 Wiki产品手册代码仓库
```

它们之间当然会共享：

```
embeddingvector searchreranking
```

等技术。

但是：

```
RAG= retrieve external knowledgeMemory= retrieve relevant past state / experience
```

**数据的来源和生命周期完全不同。**

这也是为什么简单说：

> “Memory = RAG”

会遗漏大量真正困难的问题。

---

# 十七、如果从真实产品倒推，Memory 的价值可以压缩成四句话

### 没有短期 Memory

Agent：

> 不知道刚才发生了什么。

### 没有长期 Memory

Agent：

> 每一次见你都像第一次见。

### 没有经验 Memory

Agent：

> 同样的坑可以犯一百遍。

### 没有良好的 Memory 管理

Agent：

> 虽然什么都记住了，但是已经分不清什么是真的、什么是旧的、什么现在有用。

因此真正成熟的 Agent，不是：

```
LLM + Tools
```

而更接近：

```
             Agent               │     ┌─────────┼─────────┐     ↓         ↓         ↓ Reasoning    Tools     State                         │             ┌───────────┴───────────┐             ↓                       ↓      Working / Session        Long-term Memory                                      │                         ┌────────────┼────────────┐                         ↓            ↓            ↓                       User       Experience     Shared                      Memory       Memory        Memory
```
