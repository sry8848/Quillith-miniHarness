需要时有用但一般不需要的文件位置

- `D:\learn_claudecode\harbor-lab`：测评框架。
- `D:\learn_claudecode\learn-claude-code-new`：学习教程。
- `D:\learn_claudecode\claude-code-cli`：开源泄露的 Claude Code 源码。

## 你在设计或审查异常与日志处理时，遵循以下规则：**

1. **先判断异常，再决定处理方式**
- 可预期的业务异常：明确抛出业务异常，返回清晰业务信息，一般不记 `ERROR`。

- 可恢复异常：只有当前层能真正恢复时才 `catch`，例如重试、降级、fallback，并按严重程度记录 `WARN/ERROR`。

- 未知系统异常：不要在中间层随意吞掉，继续向上抛；与用户讨论并经过同意后，在当前执行链的最终边界统一记录完整 `ERROR` 日志，并返回安全的兜底错误。
2. **日志原则**
- 日志用于定位问题，不等于异常处理。

- 同一异常传播链避免重复 `log + throw`。

- 真正处理异常或到达系统边界时再记录日志。

- `ERROR` 必须保留完整异常堆栈，例如 `log.error("创建订单失败, orderId={}", orderId, e)`。

- 日志记录关键上下文，如 `userId、orderId、requestId`，但不要记录密码、Token 等敏感信息。
3. **快速失败与兜底**
- 核心业务无法继续：快速失败，例如库存不足、支付失败、数据状态非法。

- 非核心功能失败且存在合理替代方案：允许降级兜底，例如推荐服务失败返回默认推荐。但必须经过用户明确同意

- 禁止把失败兜底成“成功”。
4. **禁止**
- 不要为了“考虑所有异常”而枚举所有 Exception。

- 不要每层都 `try-catch`。

- 不要每个异常都单独打日志。

- 不要空 `catch` 或只打印日志后继续执行错误业务。

- 不要把所有异常统一转换成业务成功。
5. **不静默吞异常**
- 所有静默吞异常的行为比如兜底，降级，转换，必须在用户明确确认下执行。你可以主动建议，但最终兜底降级策略必须与用户讨论并经过明确同意后才能做。

示例：

```
// 业务异常：快速失败if (stock < count) {    throw new StockNotEnoughException("库存不足");}
```

```
// 可恢复异常：降级try {    return recommendService.query(userId);} catch (TimeoutException e) {    log.warn("推荐服务超时, userId={}", userId, e);    return defaultRecommend();}
```

```
// 未知异常：系统边界统一处理@ExceptionHandler(Exception.class)public Result<?> handleException(Exception e) {    log.error("未处理系统异常", e);    return Result.error("系统繁忙，请稍后重试");}
```

最终判断标准：

**能处理就处理；不能处理就抛；能合理降级才兜底；未知异常在执行边界记录日志并统一失败响应；避免重复日志和吞异常。最终异常处理原则需要明确经过用户同意才能执行

## "D:\learn_claudecode\demo\Quillith-miniHarness\docs"文件夹与长期记忆规则

目录大致如下：

```Plain
docs/├─ 设计/├─ 需求/└─ 经验/   ├─ 错误记录/   ├─ 沉淀经验/   └─ SOP/
```

1. ## 设计文档

设计文档记录系统或模块“现在应该是什么样”，以及设计的演化历史。

可以记录：

- 前端交互

- 接口设计

- 模块流程

- 业务规则

- 架构和实现约束

- 代码无法直接表达的设计意图

设计文档不能直接覆盖旧版本。

第一次创建时可以只是一个文件：

```Plain
订单取消设计_2026-09-01_15-30.md
```

如果之后设计发生变化，则将该设计整理成目录，保留所有历史版本。

```Plain
订单取消设计/├─ 订单取消设计_2026-09-01_15-30.md└─ 订单取消设计_2026-09-03_10-20.md
```

新增版本时：

- 新增一份带时间的完整设计文档，历史设计文档不修改、不删除。

- 每个设计主题在有变更的情况下只维护 **一个固定的变更记录文档**，例如 `订单设计变更记录.md`，文件名不需要时间。

- 每次新增设计版本时，在该变更记录中追加一条记录，说明：
  
  - 变更时间；
  
  - 上一个设计文档；
  
  - 当前设计文档；
  
  - 当前版本相较上一版本新增、删除或修改了什么；
  
  - 必要时记录变更原因。

例如：

```Plain
订单设计/├─ 订单设计_2026-09-01_10-00.md├─ 订单设计_2026-09-03_15-20.md├─ 订单设计_2026-09-10_18-30.md└─ 订单设计变更记录.md
```

`订单设计变更记录.md` 按时间持续追加所有版本之间的变化。

原则：**设计文档记录每个时间点的完整设计，变更记录集中保存整个设计的演化过程。**

---

2. ## 需求文档

需求文档负责保存一次需求的完整思考上下文，而不只是最终需求描述。

主要包括：

- 需求描述

- 调研与决策过程

- 需求评审，价值预估

### 问题与子需求

一个需求通常会包含多个问题。

这些问题可以直接写在当前需求文档里，不需要每个问题单独创建文件。

例如：

```Plain
需要解决的问题：1. 哪些订单状态允许取消？2. 已支付订单如何退款？3. 前端按钮何时展示？
```

只有当某个问题已经变成一个相对独立、复杂度较高的事项时，才将它拆成子需求。

尤其是：

- 它需要独立调研；

- 它包含多个自己的问题；

- 它可以独立规划和执行；

- 或者它虽然是在当前需求中发现的，但实际上已经不太属于原需求本身。

此时可以在当前需求目录下建立子需求目录，形成树状结构。

不要因为“存在子问题”就机械创建新文件。

---

3. ## 技术评审文档

技术评审文档负责在需求明确之后、进入具体代码执行之前，对需求进行技术层面的评审和方案设计。

主要包括：

```Plain
概念逻辑模型设计边界条件定义代码实现方案性价比评估
```

技术评审文档关注的是“应该采用什么技术方案，以及为什么采用这个方案”。

其中：

概念模型设计用于用于在概念上描述实现逻辑、抽象模块交互、不变量、变量、共享部分等等

边界条件定义用于明确方案的适用范围、异常情况、特殊场景以及不处理的情况，防止实现过程中不断隐式扩大需求范围。

代码实现方案用于将概念模型设计转换为代码设计。

性价比评估用于判断方案的实现成本、复杂度、维护成本和实际收益，必要时比较多个方案并说明最终选择。

技术评审文档和执行文档不同。

技术评审文档不需要说明具体删除哪段代码、修改哪个方法、增加什么具体代码，也不要求详细到执行 AI 可以直接照着修改代码。

原则：技术评审文档解决“怎么设计和为什么这样设计”，执行文档解决“具体怎么改代码”。

技术评审文档应放在对应需求或子需求目录下，使需求背景、技术决策和后续执行方案能够互相追溯。

---

4. ## 执行文档

执行文档是规划 AI 交给执行 AI 的直接代码修改说明。

它和普通方案文档不同，应该非常具体，详细程度应接近：

> 执行 AI 不需要重新设计方案，只需要按照文档修改代码。

执行文档应避免纯文字描述，而是直接写出删除什么代码，新增什么代码。要详细到直接有代码或者伪代码。如复杂到有具体执行步骤，应该按依赖顺序执行。

一个需求可能有 1～N 个执行文档。

执行文档应放在对应需求或子需求目录下，使需求上下文、问题分析和代码执行方案能够互相追溯。

---

4. ## 错误记录

`经验/错误记录/` 用于记录 AI 在实际执行代码过程中走过的错误路径。

重点不是记录所有普通报错，而是记录有复用价值的：

```Plain
尝试了什么→ 为什么不行→ 最终采用什么方式解决
```

例如：

- 某种实现路径实际不可行

- 对现有代码或框架产生了错误理解

- 某次修改导致隐藏副作用

- 换了一种实现方式后成功解决

错误记录按天聚合，不为每个错误单独创建文件。

```Plain
经验/└─ 错误记录/   ├─ 2026-09-01.md   └─ 2026-09-02.md
```

同一天的新错误继续追加到当天文件中，可以在每条记录内部注明具体时间。

---

5. ## 沉淀经验与 SOP

`沉淀经验/` 暂时不由当前编码 AI 主动维护，它会由其他模型根据历史材料进一步总结。

`SOP/` 由人维护。

除非明确要求，否则编码 AI 不应主动修改 SOP。

---

6. ## 命名与维护原则

除按天聚合的错误记录外，需要标题的文档名称应包含：

```Plain
标题_YYYY-MM-DD_HH-mm
```

例如：

```Plain
订单取消需求_2026-09-01_15-40.md退款机制执行_2026-09-01_16-05.md订单状态设计_2026-09-02_10-20.md
```

错误记录只按日期命名：

```Plain
2026-09-01.md
```

必须遵循以下原则：

1.在任何有必要的场景下，应该积极更新文档。

2.所有文档更新操作必须经过用户同意。

3.主动说明更新意愿。
