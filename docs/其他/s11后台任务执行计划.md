# s11 后台 Bash 执行计划

## 1. 实施结论

s11 为现有 bash 工具增加 run_in_background 布尔字段，并建立前台调度和后台调度两条独立路径。

固定契约：

1. 父 Agent 和子 Agent 都向模型暴露同一份 bash 工具 Schema。
2. command 保持必填字符串。
3. run_in_background 缺失和等于 false 时，调用进入现有前台并发调度器。
4. run_in_background 等于 true 时，调用直接进入后台任务调度器，不建立前台顺序依赖。
5. 前台调度器只接收普通工具调用。
6. 后台调度器只接收后台工具调用。
7. 两条路径都调用同一个公共工具执行管线。
8. 后台调用返回任务 ID 后，真实 Bash 命令继续在后台运行。
9. 每个 AgentLoop 持有独立的后台状态 Map、完成通知 Queue 和后台任务 Hook。
10. 全部后台任务都阻止所属 AgentLoop 最终回答。
11. 第一版沿用 120 秒超时和 50,000 字节输出上限。
12. 第一版不提供任务查询、取消、持久化、重试、主动唤醒和运行中任务表。

run_in_background=true 表达严格前提：命令在当前模型响应中收到完整参数后立刻启动，不依赖同一响应内前后普通工具的执行结果。

## 2. 调度模型

### 2.1 两条调度路径

    完整 tool_use 输入
            ↓
    ToolCallDispatcher 读取执行模式
            ├─ FOREGROUND
            │      ↓
            │  ToolExecutionScheduler
            │      ↓
            │  AgentLoop.executeTool()
            │      ↓
            │  同步工具结果
            │
            └─ BACKGROUND
                   ↓
               BackgroundTaskScheduler
                   ↓
               AgentLoop.executeTool()
                   ↓
               BashTool 登记真实后台命令
                   ↓
               启动确认 tool_result
                   ↓
               Map 状态表 + 完成通知 Queue

AgentLoop.executeTool() 保持公共工具执行管线职责：

1. BeforeToolUse Hook。
2. 权限确认。
3. 工具输入修改。
4. ToolRegistry.execute()。
5. AfterToolUse Hook。
6. 工具输出修改。
7. 工具管线异常转换。

调度器只决定公共管线的启动时机，不拥有权限、日志和 Hook 规则。

### 2.2 同轮 A、B、C、D、E

模型输出：

1. 前台并发安全任务 A。
2. 前台并发安全任务 B。
3. 后台任务 C。
4. 前台独占任务 D。
5. 后台任务 E。

固定执行链路：

    A ─┐
       ├─ 前台并发批次启动
    B ─┘
    
    C ─── 后台任务调度器立即启动
    E ─── 后台任务调度器立即启动
    
    D ─── 等待 A 和 B 结束后启动

C 和 E 不进入 ToolExecutionScheduler，D 不等待 C 和 E 的真实命令结束。

本轮 tool_result 按模型输出顺序交付：

1. A 的真实结果。
2. B 的真实结果。
3. C 的 bg_0001 已启动。
4. D 的真实结果。
5. E 的 bg_0002 已启动。

collectToolResults() 只等待 C 和 E 的启动确认 Future，不等待 bg_0001 和 bg_0002 的真实 Bash Future。

### 2.3 后台命令顺序契约

后台字段不保留同轮工具调用的先后依赖。

    依赖前台工具副作用的命令：
    前台工具结束
            ↓
    下一模型轮次
            ↓
    发起后台命令
    
    不依赖前台工具副作用的命令：
    同轮直接设置 run_in_background=true

本次不分析 Bash 文本，不推断文件依赖，不建设路径锁。模型填写 run_in_background=true 时承担命令可立即启动的编排责任。

## 3. 父子 Agent 的边界

父子 Agent 的工具能力相同，后台状态独立。

    父 AgentLoop
      ├─ 父 BashTool
      ├─ 父 BackgroundTaskScheduler
      └─ 父 BackgroundTaskHook
    
    TaskTool 同步调用
      └─ 子 AgentLoop
           ├─ 子 BashTool
           ├─ 子 BackgroundTaskScheduler
           └─ 子 BackgroundTaskHook

| 项目                      | 父子关系 | 原因                         |
| ----------------------- | ---- | -------------------------- |
| bash 工具名称和 Schema       | 相同   | 两个模型拥有相同的 Bash 调度能力        |
| run_in_background 语义    | 相同   | 两个模型遵守同一调度契约               |
| BackgroundTaskScheduler | 独立   | 完成结果必须回到启动任务的消息历史          |
| 状态 Map 和完成 Queue        | 独立   | 防止父子模型消费彼此的任务结果            |
| BackgroundTaskHook      | 独立实例 | 每个 AgentLoop 在自身停止边界等待自身任务 |
| 工作区路径和 Git Bash 路径      | 相同配置 | 父子运行在同一工作区                 |
| 权限策略和工具日志策略             | 相同实现 | 所有工具调用统一经过公共工具执行管线         |

当前 TaskTool 同步等待子 AgentLoop 返回。子 Agent 在自身后台任务全部完成并读取结果后返回最终结论，父 Agent 接收该结论后继续自身流程。

## 4. 文件变更清单

### 4.1 新增生产代码

    demo/manual-agent/src/main/java/dev/learn/agent/manual/background/
    └── BackgroundTaskScheduler.java
    
    demo/manual-agent/src/main/java/dev/learn/agent/manual/hook/hooks/
    └── BackgroundTaskHook.java
    
    demo/manual-agent/src/main/java/dev/learn/agent/manual/tool/
    ├── ToolExecutionMode.java
    └── ToolCallDispatcher.java

### 4.2 修改生产代码

    demo/manual-agent/src/main/java/dev/learn/agent/manual/AgentLoop.java
    demo/manual-agent/src/main/java/dev/learn/agent/manual/ManualAgentApplication.java
    demo/manual-agent/src/main/java/dev/learn/agent/manual/tool/AgentTool.java
    demo/manual-agent/src/main/java/dev/learn/agent/manual/tool/ToolRegistry.java
    demo/manual-agent/src/main/java/dev/learn/agent/manual/tool/tools/BashTool.java
    INTERVIEW_REVIEW.md

### 4.3 新增测试代码

    demo/manual-agent/src/test/java/dev/learn/agent/manual/background/
    └── BackgroundTaskSchedulerTest.java
    
    demo/manual-agent/src/test/java/dev/learn/agent/manual/hook/hooks/
    └── BackgroundTaskHookTest.java
    
    demo/manual-agent/src/test/java/dev/learn/agent/manual/tool/
    └── ToolCallDispatcherTest.java

### 4.4 明确不修改

    ToolExecutionScheduler.java
    HookRegistry.java
    HookEffect.java
    ContextManager.java
    ToolDefinitionFactory.java
    TaskTool.java
    task 包与五个 s10 任务工具

ToolExecutionScheduler 保留现有实现和测试。s11 通过 ToolCallDispatcher 决定哪些调用提交给它，不在该类中加入后台分支。

## 5. ToolExecutionMode、AgentTool 与 ToolRegistry

### 5.1 ToolExecutionMode.java

新增枚举：

    public enum ToolExecutionMode {
        FOREGROUND,
        BACKGROUND
    }

该枚举只表达工具调用进入哪条调度路径。它不表达并发安全属性、权限决策和真实命令成功状态。

### 5.2 AgentTool.executionMode(JsonNode input)

在 AgentTool 增加默认方法：

    default ToolExecutionMode executionMode(
            JsonNode input
    ) {
        return ToolExecutionMode.FOREGROUND;
    }

所有现有工具沿用默认前台模式。isConcurrencySafe() 继续只服务于前台调度器。

### 5.3 ToolRegistry.executionMode(ToolCall toolCall)

新增方法：

    public ToolExecutionMode executionMode(
            ToolCall toolCall
    )

执行步骤：

1. 根据 toolCall.name() 查找注册工具。
2. 未注册工具返回 FOREGROUND。
3. 已注册工具调用 tool.executionMode(toolCall.input())。
4. ToolRegistry.execute() 保持现有 unknown tool 失败结果。

未知工具仍进入前台路径，无法绕过前台安全边界。

## 6. ToolCallDispatcher.java

### 6.1 职责

ToolCallDispatcher 是一次流式模型响应的调度分流器。

字段：

    private final ToolRegistry toolRegistry;
    private final ToolExecutionScheduler foregroundScheduler;
    private final BackgroundTaskScheduler backgroundScheduler;

职责：

1. 读取完整 ToolCall 的执行模式。
2. 前台调用提交给 ToolExecutionScheduler。
3. 后台调用提交给 BackgroundTaskScheduler。

本类不执行 Hook，不解析 Anthropic 流，不构造 tool_result，不保存真实 Bash 结果。

### 6.2 构造函数

    public ToolCallDispatcher(
            ToolRegistry toolRegistry,
            BackgroundTaskScheduler backgroundScheduler
    )

构造函数内部创建新的 ToolExecutionScheduler。该前台调度器只覆盖当前一次流式模型响应。

### 6.3 submit(ToolCall toolCall, Supplier<ToolExecutionResult> action)

    public CompletableFuture<ToolExecutionResult> submit(
            ToolCall toolCall,
            Supplier<ToolExecutionResult> action
    )

固定分流逻辑：

    return toolRegistry.executionMode(toolCall)
            == ToolExecutionMode.BACKGROUND
            ? backgroundScheduler.submitLaunch(action)
            : foregroundScheduler.submit(
                    toolRegistry.isConcurrencySafe(toolCall.name()),
                    action
            );

AgentLoop 固定传入：

    () -> executeTool(completedTool)

前台和后台都执行同一个 executeTool()，因此权限、日志、输入修改、输出修改和异常转换没有分叉实现。

### 6.4 close()

ToolCallDispatcher 实现 AutoCloseable。

    @Override
    public void close()

关闭时只调用 foregroundScheduler.close()。后台任务调度器跨模型响应持续存在，直到应用退出才关闭。

## 7. BackgroundTaskScheduler.java

### 7.1 职责

BackgroundTaskScheduler 同时承担后台启动和后台任务生命周期管理：

1. 立即调度后台工具调用的启动动作。
2. 生成后台任务 ID。
3. 在虚拟线程中执行真实 Bash。
4. 保存运行中任务。
5. 保存已完成结果。
6. 提供完成通知队列。
7. 在 Stop Hook 中等待全部未完成任务。
8. 关闭后台执行器。

本类不持有消息历史，不调用模型，不构造 Anthropic 内容块，不直接触发 Hook。

### 7.2 字段和状态类型

    private final ExecutorService executor;
    private final ConcurrentMap<String, RunningTask> runningTasks;
    private final Queue<CompletedTask> completedTasks;
    private final AtomicLong nextTaskNumber;
    
    private static final class RunningTask {
        private final String id;
        private final String command;
        private final CompletableFuture<Void> completion;
    }
    
    public record CompletedTask(
            String id,
            String command,
            String status,
            String output
    ) {
    }

runningTasks 只保存运行任务。命令结束时，调度器先将 CompletedTask 放入 Queue，再从 Map 移除运行记录。

### 7.3 submitLaunch(Supplier<ToolExecutionResult> action)

    public CompletableFuture<ToolExecutionResult> submitLaunch(
            Supplier<ToolExecutionResult> action
    )

执行步骤：

1. 立刻把 action 提交给 executor。
2. 不建立前台顺序依赖。
3. 返回启动确认 Future。
4. AgentLoop.collectToolResults() 只等待该 Future。

action 固定执行公共工具管线。权限拒绝、输入错误和工具管线失败均在该 Future 中返回失败 tool_result，不生成后台任务记录。

### 7.4 start(String command, Supplier<ToolExecutionResult> commandAction)

    public String start(
            String command,
            Supplier<ToolExecutionResult> commandAction
    )

执行步骤：

1. 使用 AtomicLong 生成 bg_0001 格式任务 ID。
2. 创建 RunningTask。
3. 先放入 runningTasks。
4. 提交真实命令虚拟线程。
5. 立刻返回任务 ID。

真实命令线程固定执行：

    commandAction.get()
            ↓
    error=false → completed
    error=true  → failed
            ↓
    生成 CompletedTask
            ↓
    CompletedTask 进入 completedTasks
            ↓
    runningTasks 删除当前任务
            ↓
    完成 completion Future

RuntimeException 在该工作线程边界转换成 ToolExecutionResult.failure()。该转换确保任务结束结果必定进入完成 Queue。

### 7.5 状态与交付方法

    public boolean hasOutstandingTasks()

返回条件：

    !runningTasks.isEmpty()
            || !completedTasks.isEmpty()

完成但尚未交付的任务继续阻止 AgentLoop 最终返回。

    public void awaitAllCompleted()

执行步骤：

1. 读取当前 runningTasks 的全部 completion Future。

2. 使用 CompletableFuture.allOf() 生成联合完成点。

3. 调用 join()。

4. 不创建轮询线程，不调用 Thread.sleep()，不触发模型请求。
   
    public List<CompletedTask> collectCompleted()

执行步骤：

1. 按 Queue 顺序取出全部 CompletedTask。
2. 放入不可修改列表。
3. 返回本次列表。

Queue 中的结果只会被取出一次，因此每项后台结果只交付一次。

### 7.6 close()

    @Override
    public void close()

关闭时调用 executor.shutdownNow()。真实 Bash 进程由对应 BashTool.close() 终止。

## 8. BashTool.java

### 8.1 构造函数和工具定义

BashTool 构造函数增加后台任务调度器：

    public BashTool(
            Path workspace,
            Path bashExecutable,
            BackgroundTaskScheduler backgroundScheduler
    )

工具名称保持 bash，新增非必填布尔属性：

    {
      "command": "mvn test",
      "run_in_background": true
    }

Schema 规则：

1. command 是必填字符串。
2. run_in_background 是非必填布尔值。
3. 字段缺失等同于 false。
4. 字段为非布尔值时返回工具失败。

工具描述固定写明：

    Set run_in_background=true only for a command that may start immediately.
    The command result is delivered to this agent before its final answer.

ToolDefinitionFactory 保持不变。布尔 Schema 直接在 BashTool 的属性 Map 中使用 JsonValue.from(...) 创建。

### 8.2 executionMode(JsonNode input)

BashTool 覆盖 AgentTool.executionMode()：

    @Override
    public ToolExecutionMode executionMode(
            JsonNode input
    )

固定判断：

    run_in_background 是布尔 true → BACKGROUND
    字段缺失 → FOREGROUND
    字段为布尔 false → FOREGROUND
    字段为非布尔值 → FOREGROUND

非布尔值进入前台公共工具管线后由 execute() 返回参数失败结果，不启动后台任务。

### 8.3 execute(JsonNode input)

执行步骤：

1. 读取并校验 command。

2. 读取并校验 run_in_background。

3. 字段缺失和字段为 false 时调用 executeSynchronously(command)。

4. 字段为 true 时调用 backgroundScheduler.start(command, () -> executeSynchronously(command))。

5. 后台启动成功后返回：
   
    Background task bg_0001 started. The command result will be delivered before the final answer.

BashTool.execute() 在后台模式下只登记真实任务并返回启动确认。真实命令不进入 ToolExecutionScheduler。

BashTool.isConcurrencySafe() 保持默认 false。同步 Bash 是独占前台工具，不由模型声明并发安全。

### 8.4 executeSynchronously(String command)

新增私有方法：

    private ToolExecutionResult executeSynchronously(
            String command
    )

本方法承载现有进程启动、输出读取、超时、退出码和输出截断逻辑。后台任务通过 Lambda 调用本方法，避免重新读取 run_in_background 并避免递归启动。

### 8.5 进程关闭

类声明调整为：

    public final class BashTool implements AgentTool, AutoCloseable

新增字段：

    private final Set<Process> runningProcesses =
            ConcurrentHashMap.newKeySet();

新增私有方法：

    private void stopProcess(
            Process process
    )

固定处理：

1. 终止 Bash 子进程。
2. 强制终止 Bash 进程。
3. 等待进程退出。

超时分支、线程中断分支和 close() 复用 stopProcess()。命令结束后在 finally 从 runningProcesses 移除。

## 9. AgentLoop.java

### 9.1 新增依赖

构造函数增加：

    BackgroundTaskScheduler backgroundScheduler

字段只服务于 ToolCallDispatcher 的创建。AgentLoop 不读取后台状态 Map，不格式化后台通知，不调用 awaitAllCompleted()。

### 9.2 流式响应内的提交

createStreamingTurn() 创建 ToolCallDispatcher：

    try (ToolCallDispatcher toolDispatcher =
                 new ToolCallDispatcher(
                         toolRegistry,
                         backgroundScheduler
                 )) {
        // 消费模型流
    }

合法 JSON 工具输入的固定提交代码：

    ToolCall toolCall =
            ToolCall.from(completedTool);
    
    CompletableFuture<ToolExecutionResult> future =
            toolDispatcher.submit(
                    toolCall,
                    () -> executeTool(completedTool)
            );

非法 JSON 输入继续创建现有失败 Future，不进入任一调度器。

PendingToolExecution 保持现有结构，继续按模型输出顺序保存 ToolUseBlock 和结果 Future。

### 9.3 工具结果收集

collectToolResults() 保持按模型输出顺序 join() 每个 Future。

后台调用 Future 在后台任务登记完成后结束。真实命令 Future 留在 BackgroundTaskScheduler 内部。

executeTool() 保持公共工具执行管线职责，不复制权限、日志和 Hook 调用。

## 10. BackgroundTaskHook.java

### 10.1 职责

BackgroundTaskHook 将一个 AgentLoop 的后台任务状态接入现有生命周期。

    public BackgroundTaskHook(
            BackgroundTaskScheduler backgroundScheduler
    )

### 10.2 beforeModelCall(List<MessageParam> messages)

执行步骤：

1. 调用 backgroundScheduler.collectCompleted()。
2. Queue 为空时返回 HookEffect.proceed()。
3. Queue 存在结果时格式化全部通知。
4. 使用 HookEffect.addContext() 返回一段通知上下文。

该 Hook 不注入运行中任务表。

### 10.3 onStop(List<MessageParam> messages)

执行步骤：

1. hasOutstandingTasks() 为 false 时返回 HookEffect.proceed()。
2. 存在任务时调用 awaitAllCompleted()。
3. 调用 collectCompleted()。
4. 返回 HookEffect.Decision.BLOCK。
5. reason 固定为“后台命令已经全部结束，请根据执行结果继续处理。”。
6. additionalContexts 保存本批任务通知。

现有 AgentLoop 收到 BLOCK 后把原因和通知写入下一条 user 消息并再次调用模型。模型读取全部后台结果后进行最终回答。

### 10.4 通知格式

    <task_notification>
      <task_id>bg_0001</task_id>
      <status>completed</status>
      <command>mvn test</command>
      <result>Exit code: 0
    ...</result>
    </task_notification>

status 固定使用 completed 和 failed。result 使用 Bash 已经限制到 50,000 字节的输出，不增加第二个截断阈值。

formatNotifications(List<CompletedTask> tasks) 是 Hook 内部私有方法，供 beforeModelCall() 和 onStop() 共用。

## 11. Hook 与调度边界

后台 Bash 固定顺序：

    ToolCallDispatcher 选择 BACKGROUND
            ↓
    BackgroundTaskScheduler.submitLaunch
            ↓
    AgentLoop.executeTool
            ↓
    BeforeToolUse
            ↓
    PermissionHook
            ↓
    BashTool.start
            ↓
    AfterToolUse 接收启动确认
            ↓
    原始 tool_use_id 收到启动确认 tool_result
            ↓
    真实 Bash 在后台结束
            ↓
    CompletedTask 进入完成 Queue
            ↓
    BackgroundTaskHook 交付真实结果

真实 Bash 结束后不再次触发 AfterToolUse。真实结果属于后台任务通知，不属于第二次原始工具调用结果。

PermissionHook 的终端确认锁继续串行化交互确认。该锁只保护终端输入，不建立前台工具顺序依赖。

LargeOutputHook 处理启动确认文本。真实 Bash 输出已经受 Bash 输出上限约束，后台通知不增加新的大输出治理路径。

## 12. ManualAgentApplication.java

### 12.1 创建父子独立后台组件

    BackgroundTaskScheduler parentBackgroundScheduler =
            new BackgroundTaskScheduler();
    
    BashTool parentBashTool =
            new BashTool(
                    workspace,
                    bashExecutable,
                    parentBackgroundScheduler
            );
    
    BackgroundTaskScheduler subagentBackgroundScheduler =
            new BackgroundTaskScheduler();
    
    BashTool subagentBashTool =
            new BashTool(
                    workspace,
                    bashExecutable,
                    subagentBackgroundScheduler
            );

### 12.2 注册和装配

父 ToolRegistry 注册 parentBashTool。

子 ToolRegistry 注册 subagentBashTool。

父 Hook 注册表增加：

    new BackgroundTaskHook(
            parentBackgroundScheduler
    )

子 Hook 注册表增加：

    new BackgroundTaskHook(
            subagentBackgroundScheduler
    )

创建父 AgentLoop 时传入 parentBackgroundScheduler。

创建子 AgentLoop 时传入 subagentBackgroundScheduler。

两个 Registry 发送给模型的 bash 工具定义相同。

### 12.3 资源关闭

应用 finally 固定执行：

    parentBackgroundScheduler.close()
    subagentBackgroundScheduler.close()
    parentBashTool.close()
    subagentBashTool.close()
    client.close()
    scanner.close()

先中断后台 Java 任务，再终止真实 Bash 进程。

### 12.4 CLI 标识

    s10 Task System Agent
            ↓
    s11 Background Task Scheduler Agent
    
    s10 >>
            ↓
    s11 >>

## 13. 测试计划

### 13.1 ToolCallDispatcherTest

测试使用五个测试工具和 CountDownLatch 复现 A、B、C、D、E。

1. A 和 B 声明 FOREGROUND 与并发安全，两个动作均等待闩锁。
2. C 和 E 声明 BACKGROUND，动作立刻记录启动信号并返回启动确认。
3. D 声明 FOREGROUND 与独占，动作记录启动信号。
4. 按 A、B、C、D、E 顺序调用 submit()。
5. 断言 C 和 E 在 A、B 未释放时已经启动。
6. 断言 D 在 A、B 未释放时没有启动。
7. 释放 A 和 B。
8. 断言 D 启动。
9. 关闭分流器。

该测试证明后台调用没有进入前台顺序依赖。

### 13.2 BackgroundTaskSchedulerTest

测试一：启动任务立即返回 ID。

1. 使用 CountDownLatch 阻塞真实命令动作。
2. 调用 start()。
3. 断言立即返回 bg_0001。
4. 断言 hasOutstandingTasks() 返回 true。

测试二：任务结束进入完成 Queue。

1. 真实动作返回 ToolExecutionResult.success()。
2. 调用 awaitAllCompleted()。
3. 调用 collectCompleted()。
4. 断言状态为 completed。
5. 断言结果只出现一次。

测试三：失败结果进入完成 Queue。

1. 真实动作返回 ToolExecutionResult.failure()。
2. 调用 awaitAllCompleted()。
3. 断言状态为 failed。

测试四：多个任务全部结束后才解除等待。

1. 启动两个独立闩锁控制的任务。
2. 只释放第一个任务。
3. 断言 awaitAllCompleted() 尚未返回。
4. 释放第二个任务。
5. 断言等待结束并取得两个完成结果。

测试五：启动动作不建立前台依赖。

1. 调用 submitLaunch()。
2. 断言返回 Future 只等待启动动作。
3. 在启动动作内部调用 start()。
4. 断言启动 Future 完成时真实命令仍在运行。

### 13.3 BackgroundTaskHookTest

测试一：模型调用前交付已完成结果。

1. 创建已完成后台任务。
2. 调用 beforeModelCall()。
3. 断言附加上下文包含任务 ID、状态、命令和完整结果。
4. 再次调用 beforeModelCall()。
5. 断言不重复交付。

测试二：停止时等待全部后台任务。

1. 启动两个受闩锁控制的后台任务。
2. 在测试虚拟线程调用 onStop()。
3. 断言任务运行期间 onStop() 未返回。
4. 释放两个任务。
5. 断言 Hook 返回 BLOCK。
6. 断言附加上下文包含两个通知。

测试三：没有后台任务时允许停止。

1. 调用 onStop()。
2. 断言返回 CONTINUE。
3. 断言没有附加上下文。

### 13.4 回归验证

在 demo/manual-agent 目录执行：

    mvn test

必须通过：

1. ToolExecutionSchedulerTest。
2. ToolCallDispatcherTest。
3. BackgroundTaskSchedulerTest。
4. BackgroundTaskHookTest。
5. TaskSystemTest。
6. ContextManagerTest。

### 13.5 手动验证

父 Agent：

    同一轮启动两个互不依赖的后台命令：
    sleep 3 && echo C
    sleep 3 && echo E
    同时读取 pom.xml，随后再执行一个依赖读取结果的普通工具。
    在全部命令结果到达后总结。

观察点：

1. C 和 E 的启动日志早于普通独占任务结束日志。
2. tool_result 返回两个后台任务 ID。
3. 后台命令运行期间模型继续处理普通工具结果。
4. 最终停止前 Hook 等待 C 和 E。
5. 两条 task_notification 各出现一次。
6. 最终回答基于两条真实命令结果。

子 Agent：

    委派一个子 Agent：在后台运行 sleep 2 && echo child-done，
    读取 pom.xml，并在结果到达后给出结论。

观察点：

1. 子 Agent 看到 run_in_background 字段。
2. 子 Agent 自己等待 child-done。
3. 父 Agent 收到子 Agent 最终结论。
4. 父 Agent 的后台 Queue 不出现子 Agent 任务通知。

## 14. 实施顺序

1. 新增 ToolExecutionMode，修改 AgentTool 与 ToolRegistry。
2. 新增 BackgroundTaskScheduler 和 BackgroundTaskSchedulerTest。
3. 新增 ToolCallDispatcher 和 A、B、C、D、E 路由测试。
4. 修改 BashTool，加入 Schema、执行模式、后台启动和进程关闭。
5. 修改 AgentLoop，使用 ToolCallDispatcher 提交完整工具调用。
6. 新增 BackgroundTaskHook 和 Hook 测试。
7. 修改 ManualAgentApplication，装配父子独立实例。
8. 更新 CLI 标识。
9. 执行 Maven 回归测试。
10. 执行父 Agent 和子 Agent 手动验证。
11. 更新 INTERVIEW_REVIEW.md 中最相关段落，记录前台顺序调度和后台生命周期调度的边界。

每一步只修改直接涉及的文件。现有 s10 未提交代码不做格式调整、命名调整和结构重排。

## 15. 本次明确不实现

1. wait_background 工具。
2. 后台任务查询工具。
3. 后台任务取消工具。
4. 后台任务持久化。
5. 应用重启恢复。
6. 后台任务重试。
7. 完成后主动唤醒已结束 Agent。
8. 每轮重复注入运行中任务表。
9. 独立后台超时字段。
10. 后台并发数量限制。
11. Bash 文本依赖分析。
12. 文件路径锁。
13. 模型阶段性文本缓冲。
14. 终端文本撤回。

## 16. 完成标准

s11 完成时必须同时满足：

1. 父 Agent 和子 Agent 的 Bash Schema 都包含 run_in_background。
2. 同步 Bash 保持现有输出格式、超时和输出上限。
3. run_in_background=true 的后台真实命令不提交到 ToolExecutionScheduler。
4. 同轮 A、B、C、D、E 中，C 和 E 不等待 A、B、D。
5. D 继续等待 A 和 B。
6. 后台工具调用继续经过权限、日志和公共 Hook 管线。
7. 后台成功和失败结果各交付一次。
8. 每个 AgentLoop 只消费自身后台任务通知。
9. 每个 AgentLoop 在自身后台任务完成并交付给模型前不返回最终回答。
10. 应用退出时终止全部活动 Bash 进程。
11. 现有测试和新增测试全部通过。
12. Git diff 只包含 s11 直接需要的修改和原有 s10 工作树内容。
