# 模型请求错误恢复重构计划

## 1. 本次结论

本次只重构主模型请求输入的 `DataInspectionFailed` 恢复，不为 `Prompt Too Long` 新增 Handler。

`Prompt Too Long` 将作为普通未恢复 Provider 异常原样向外抛出。实施时同步删除现有 `requestModel()` 中的 Prompt Too Long fallback，不保留新旧双轨。

理由是当前请求前已经有完整的主动上下文治理管线：

1. `snipMiddle()` 按消息数裁剪中段历史；
2. `compactOldToolResults()` 替换较旧的大 Tool Result；
3. `shouldAutoCompact()` 按序列化消息字符数触发主动压缩；
4. `compactHistory()` 用摘要替换当前活跃历史；
5. `applyToolResultBudget()` 在本批 Tool Result 超过 200,000 字符时落盘大结果，模型上下文只保留文件引用和预览。

现有 `recoverFromPromptTooLong()` 不是一种新的恢复能力，它仍然是调用摘要模型压缩历史，只是改成“摘要旧轮次，保留最新 API 轮次”。它与主动压缩存在以下冲突：

- 如果请求前刚执行过 `compactHistory()`，活跃历史只剩一条 user 摘要，`recoverFromPromptTooLong()` 找不到可摘要的旧 assistant 轮次，会直接失败；
- 如果超限来自 system prompt、tool definitions、`turnContext` 或 Provider Token 计算差异，再摘要 messages 不一定能解决；
- 该恢复会再调用一次同一 Provider 的摘要模型，与“Handler 不调用模型”的边界冲突；
- 它只能覆盖“本地 50,000 字符阈值没触发，但 Provider 仍认为超限，且历史中还有旧轮次可压缩”这一个窄场景。

所以本次不把这个窄 fallback 包装成新 Handler。以后如果确实要处理 Prompt Too Long，应单独设计“请求预算”，统一计算 messages、system prompt、tool definitions 和临时上下文，而不是在 Provider 拒绝后再追加第二套摘要流程。

---

## 2. 本次最终控制流

```text
AgentLoop 直接发起主模型请求
        ↓
AnthropicServiceException
        ↓
recoveryManager.tryRecover(exception, state)
        ├─ true  → Handler 已完成消息修改 → AgentLoop continue
        └─ false → AgentLoop throw exception
```

`false` 包含三种情况：

- 不是当前支持的请求输入内容拒绝；
- 是内容拒绝，但没有可安全修改的 pending Tool Result；
- 恢复次数已经耗尽。

AgentLoop 不再为这三种情况分别写 throw 判断。

不新增 `RecoveryAction`、`RecoveryDecision`、`THROW` 枚举、默认异常 Handler 或 Handler 自动扫描。

---

## 3. 文件变化

### 3.1 新增四个生产文件

```text
src/main/java/dev/learn/agent/manual/recovery/
├─ ModelRequestRecoveryState.java
├─ ModelRequestRecoveryHandler.java
├─ ContentRejectionRecoveryHandler.java
└─ ModelRequestRecoveryManager.java
```

### 3.2 新增一个测试文件

```text
src/test/java/dev/learn/agent/manual/recovery/
└─ ModelRequestRecoveryManagerTest.java
```

### 3.3 修改现有文件

```text
src/main/java/dev/learn/agent/manual/AgentLoop.java
src/main/java/dev/learn/agent/manual/ManualAgentApplication.java
src/main/java/dev/learn/agent/manual/context/ContextManager.java
工具结果治理与模型错误恢复执行文档.md
INTERVIEW_REVIEW.md
```

`ContextManager.java` 只删除已失去调用点的 Prompt Too Long 异常 fallback，不改动主动压缩阈值和算法。

---

## 4. 新增 `ModelRequestRecoveryState`

该类表示当前连续模型请求错误恢复链，只保存 Content Rejection 恢复需要的三项数据：

```java
public final class ModelRequestRecoveryState {

    // AgentLoop 当前维护的真实消息历史。
    private final List<MessageParam> messages;

    // 当前连续请求拒绝链已完成的恢复次数。
    private int contentRejectionRecoveryCount;

    // 最近一批尚未被后续模型请求接受的 Tool Result ID。
    private Set<String> pendingToolUseIds = Set.of();
}
```

提供以下方法：

```java
public ModelRequestRecoveryState(List<MessageParam> messages)

public List<MessageParam> messages()

public int contentRejectionRecoveryCount()

public void incrementContentRejectionRecoveryCount()

public Set<String> pendingToolUseIds()

public void recordPendingToolResults(Collection<String> toolUseIds)
```

一次模型请求被 Provider 接受后，当前连续错误恢复链结束。`AgentLoop` 直接丢弃旧 State，并用同一份 `messages` 创建新的 State；不在 State 内提供重置方法。流中断产生的新 Tool Result 由 AgentLoop 随后记录到新 State。

State 不保存 Prompt Too Long 计数、Stream 状态、Tool Future、Client、Hook 或任意扩展 Map。

---

## 5. 新增 `ModelRequestRecoveryHandler`

该接口只表达模型请求错误的分类和恢复契约：

```java
public interface ModelRequestRecoveryHandler {

    /**
     * 判断当前模型请求异常是否归属该 Handler。
     */
    boolean supports(
            AnthropicServiceException exception
    );

    /**
     * 尝试完成恢复所需的状态修改。
     *
     * @return 已完成状态修改、可以重试时返回 true；否则返回 false
     */
    boolean tryRecover(
            AnthropicServiceException exception,
            ModelRequestRecoveryState state
    );
}
```

`supports()` 判断错误归属；`tryRecover()` 负责恢复条件、次数限制和状态修改。Handler 不调用模型，不执行 Tool。

---

## 6. 新增 `ContentRejectionRecoveryHandler`

该类实现 `ModelRequestRecoveryHandler`：

```java
public final class ContentRejectionRecoveryHandler
        implements ModelRequestRecoveryHandler {
}
```

### 6.1 固定策略

```java
// 当前百炼 Anthropic 兼容端点的请求输入内容拒绝码。
private static final String DATA_INSPECTION_FAILED =
        "DataInspectionFailed";

// 同一连续拒绝链最多恢复两次。
private static final int MAX_RECOVERIES = 2;

// Tool 已经真实执行，只是结果正文不能继续交付给模型。
private static final String RECOVERY_MESSAGE =
        "The tool call completed, but the provider rejected its result "
                + "before it reached the model. Do not repeat the same "
                + "operation. Narrow the next query, reduce its result "
                + "scope, or choose another tool.";
```

### 6.2 错误归属判断

```java
@Override
public boolean supports(
        AnthropicServiceException exception
) {
    // 外部 Provider 没有返回 JSON Body 时不属于该 Handler。
    JsonValue body = exception.body();
    if (body == null || body.isMissing() || body.isNull()) {
        return false;
    }

    // 无法按 JSON object 解析时不属于该 Handler。
    JsonNode bodyNode;
    try {
        bodyNode = body.convert(JsonNode.class);
    } catch (RuntimeException ignored) {
        return false;
    }

    // 只按根 code 字段识别当前 Provider 的内容拒绝。
    JsonNode code = bodyNode.get("code");
    return code != null
            && code.isTextual()
            && DATA_INSPECTION_FAILED.equals(code.textValue());
}
```

### 6.3 恢复条件和状态修改

```java
@Override
public boolean tryRecover(
        AnthropicServiceException exception,
        ModelRequestRecoveryState state
) {
    // 没有 pending Tool Result 时不能把用户输入或 Hook 上下文当成工具结果。
    if (state.pendingToolUseIds().isEmpty()) {
        return false;
    }

    // 次数耗尽后不再修改历史。
    if (state.contentRejectionRecoveryCount()
            >= MAX_RECOVERIES) {
        return false;
    }

    // 只有整批 ID 都在当前历史中时才允许一次性替换。
    if (!replacePendingToolResults(
            state.messages(),
            state.pendingToolUseIds(),
            RECOVERY_MESSAGE
    )) {
        return false;
    }

    // 历史修改完成后才记录恢复次数。
    state.incrementContentRejectionRecoveryCount();
    return true;
}
```

`replacePendingToolResults()` 从现有 `AgentLoop` 整体迁入 Handler，固定保持以下行为：

- 从历史尾部向前寻找 user block message；
- 只匹配 pending ID 对应的 `tool_result`；
- 保留 assistant `tool_use`、tool input 和 `tool_use_id`；
- 保留 `tool_result` block，只替换 content 并设置 `is_error=true`；
- 保留同一 user 消息中的其他 block；
- 只有命中数量等于整批 pending ID 数量时才 `messages.set(...)`；
- 部分匹配时返回 false，不提交半批修改。

主动压缩可能已经把原 Tool Result 替换成摘要，导致 pending ID 在当前历史中无法完整匹配。此时 Handler 返回 false。

---

## 7. 新增 `ModelRequestRecoveryManager`

Manager 保存有序 Handler 列表，并把异常交给第一个支持它的 Handler：

```java
public final class ModelRequestRecoveryManager {

    // 按注册顺序匹配的模型请求恢复 Handler。
    private final List<ModelRequestRecoveryHandler> handlers;

    /**
     * 使用固定顺序的 Handler 创建恢复分派器。
     */
    public ModelRequestRecoveryManager(
            List<ModelRequestRecoveryHandler> handlers
    ) {
        this.handlers = List.copyOf(
                Objects.requireNonNull(
                        handlers,
                        "模型请求恢复 Handler 列表不能为空"
                )
        );
    }

    /**
     * 尝试恢复一次主模型请求错误。
     *
     * @return Handler 已完成状态修改时返回 true，否则返回 false
     */
    public boolean tryRecover(
            AnthropicServiceException exception,
            ModelRequestRecoveryState state
    ) {
        // 按显式注册顺序查找第一个支持当前异常的 Handler。
        for (ModelRequestRecoveryHandler handler : handlers) {
            if (handler.supports(exception)) {
                return handler.tryRecover(
                        exception,
                        state
                );
            }
        }

        // 没有 Handler 支持时由 AgentLoop 抛出原异常。
        return false;
    }
}
```

第一个匹配 Handler 返回 false 时，Manager 直接返回 false，不继续调用后续 Handler。Manager 不识别任何具体 Provider 错误。

---

## 8. 修改 `AgentLoop`

### 8.1 注入 Manager

新增字段和构造参数：

```java
// 只尝试恢复可恢复的主模型请求错误。
private final ModelRequestRecoveryManager recoveryManager;
```

### 8.2 在 `run()` 中创建初始 State

删除现有 `pendingToolUseIds` 局部列表，改由 State 保存。现有 `contentRejectionCount` 局部变量保留，它后续只供原有 SSE 输出拒绝使用。

```java
// 初始 State 表示第一条连续模型请求错误恢复链。
ModelRequestRecoveryState requestRecoveryState =
        new ModelRequestRecoveryState(messages);

// 保留原有 SSE 输出拒绝计数，不并入请求 Handler。
int contentRejectionCount = 0;
```

### 8.3 删除 `requestModel()` 并内联真实请求

完整删除：

```java
private StreamTurn requestModel(
        List<MessageParam> messages,
        String turnContext
)
```

原调用点改成：

```java
// AgentLoop 仍然是唯一构造并发起主模型请求的地方。
try {
    turn = createStreamingTurn(
            createRequest(
                    messages,
                    turnContext
            )
    );
} catch (AnthropicServiceException exception) {
    // Handler 已完成恢复时，回到同一主循环重试。
    if (recoveryManager.tryRecover(
            exception,
            requestRecoveryState
    )) {
        continue;
    }

    // 未识别、不可恢复或次数耗尽均原样抛出。
    throw exception;
}
```

这里不再出现 Prompt Too Long、DataInspectionFailed、HTTP 状态码或恢复次数判断。

### 8.4 模型请求被接受后替换 State

在 `turn.providerError()` 分支之后、`turn.interrupted()` 判断之前调用：

```java
// 本次请求已被 Provider 接受：旧恢复链结束，后续错误从新 State 开始。
requestRecoveryState =
        new ModelRequestRecoveryState(messages);

// 保留原有 SSE 连续拒绝计数重置。
contentRejectionCount = 0;
```

### 8.5 记录新的 pending Tool Result

正常 Tool Result 成对提交后，以及 `commitInterruptedTurn()` 提交中断前 Tool Result 后，统一调用：

```java
// 这批 Tool Result 要等下一次模型请求被接受后才结束 pending。
requestRecoveryState.recordPendingToolResults(
        turn.toolExecutions()
                .stream()
                .map(execution -> execution.toolUse().id())
                .toList()
);
```

### 8.6 SSE 原有处理保持不变

本次不新增 SSE Handler，不让 SSE 异常进入 `ModelRequestRecoveryManager`，不改名 Stream 方法或常量。

只对现有混合方法 `recoverFromProviderError()` 做两个必要的外科手术式修改：

1. 删除 `providerErrorTurn == null` 的 HTTP/input 分支，因为该分支已迁入请求 Handler；
2. 该方法原本对 `pendingToolUseIds` 的 clear/add 改为调用 `requestRecoveryState.recordPendingToolResults(...)`，只是状态存放位置迁移。

除这两点外，以下逻辑保持原样：

- `SseException` 在 `createStreamingTurn()` 内转成 `StreamTurn.providerError`；
- partial assistant blocks 和已完整收到的 tool_use；
- 已启动 Tool Future 的等待和事实提交；
- `commitProviderErrorToolFacts()`；
- `ContentRejectionDirection`、Provider 错误解析私有方法和现有两次上限；
- `AnthropicIoException` 和 `AnthropicInvalidDataException` 的 `interruptedTurn()` 处理。

### 8.7 从 `AgentLoop` 删除的内容

```text
requestModel
isPromptTooLong
replacePendingToolResults
toolResultContentRejectionMessage
HTTP/input 分支使用的恢复常量
```

`ContentRejectionDirection`、`providerErrorCode()`、`providerErrorMessage()`、`providerErrorSummary()`、`providerErrorBody()` 和 `readBodyString()` 暂时保留，因为现有 SSE 输出拒绝分支仍在使用。本次不借机重构它们。

删除不再使用的 imports；不调整无关代码格式。

---

## 9. 修改 `ContextManager`

完整删除已没有生产调用点的：

```text
recoverFromPromptTooLong
findLatestApiRoundStart
```

同步修改两处失效注释：

1. `shouldAutoCompact()` 不再声称“后续由 Prompt Too Long 恢复入口兜底”，改为明确说明本地字符阈值不能保证 Provider 一定接受；
2. `compactMessages()` 不再声称“正常压缩和错误恢复复用”，只描述现有主动压缩。

以下方法和常量保持原样：

```text
snipMiddle
compactOldToolResults
shouldAutoCompact
compactHistory
compactMessages
summarizeHistory
applyToolResultBudget
TARGET_MESSAGE_COUNT
TOOL_RESULT_BUDGET_CHARACTERS
AUTO_COMPACT_CHARACTER_THRESHOLD
SUMMARY_INPUT_CHARACTER_LIMIT
```

本次不重新调整 50,000 和 200,000 字符阈值，不增加 Tokenizer，不重写主动压缩算法。

---

## 10. 修改 `ManualAgentApplication`

在 `ContextManager` 创建完成后增加：

```java
// Content Handler 只保存无运行状态的恢复规则。
ContentRejectionRecoveryHandler contentRejectionHandler =
        new ContentRejectionRecoveryHandler();

// Handler 列表的显式顺序就是错误匹配优先级。
ModelRequestRecoveryManager recoveryManager =
        new ModelRequestRecoveryManager(
                List.of(
                        contentRejectionHandler
                )
        );
```

父、子两个 `new AgentLoop(...)` 都注入同一个无状态 Manager。每次 `run()` 创建自己的 State，不共享计数和 pending ID。

CLI 的 `printProviderError()`、`rollbackFailedTurn()` 和现有错误输出私有方法全部保持不变。

---

## 11. 测试计划

测试以真实执行链路为主，单元测试只保留不能从集成结果清楚定位的策略契约。所有测试方法先用 BDD 注释描述行为。

1. `ModelRequestRecoveryManagerTest` 验证列表分派：按注册顺序选择第一个支持异常的 Handler；该 Handler 返回 false 后立即结束；没有 Handler 支持时返回 false。
2. `ContentRejectionRecoveryHandlerTest` 验证策略边界：只识别 Body 根 `code=DataInspectionFailed`；完整 pending 批次可原子替换；没有 pending、批次不完整或次数耗尽时不修改历史和计数。
3. 新增 `AgentLoopModelRequestRecoveryIntegrationTest`，通过本地 HTTP 测试服务和真实 Anthropic SDK 请求链路依次返回成功的 `tool_use` SSE、`DataInspectionFailed`、成功结束 SSE。使用测试 Tool 记录真实执行次数，断言 Tool 只执行一次，AgentLoop 在同一执行循环中发起恢复请求；恢复请求保留 assistant `tool_use`、tool input 和 `tool_use_id`，仅替换 pending Tool Result body。
4. 同一集成测试增加两个不可恢复场景：没有 pending Tool Result，以及连续恢复次数耗尽。两者都断言原始 `AnthropicServiceException` 向外抛出，并且不会多发一次模型请求。
5. 集成测试让一次请求成功后再形成新的 pending 批次并触发拒绝，证明新 State 从零计数，旧恢复链不再影响新链。

本次不为 SSE/partial turn 新建恢复测试框架；运行现有测试确认原有流式处理没有回归。

---

## 12. 实施顺序

1. 新增 `ModelRequestRecoveryState`、`ModelRequestRecoveryHandler`、`ContentRejectionRecoveryHandler` 和 `ModelRequestRecoveryManager`，并完成少量策略测试。
2. 在 `ManualAgentApplication` 按显式顺序注册 Handler，把同一个 Manager 注入父子 AgentLoop。
3. 修改 `AgentLoop`：创建请求恢复 State；内联 `createStreamingTurn(createRequest(...))`；HTTP Provider catch 只执行 `tryRecover() → continue / throw exception`；模型请求被接受后创建新 State；Tool Result 提交后记录新的 pending ID。
4. 从 `recoverFromProviderError()` 移除 HTTP/input 恢复分支，保留 SSE/partial turn 的现有状态机；删除 Prompt Too Long 异常恢复入口及失效注释、常量和 import。
5. 增加真实 SDK 请求链路的 AgentLoop 集成测试，覆盖恢复成功、不可恢复和成功后开启新恢复链。
6. 运行 `mvn -q clean test`、`git diff --check`，并搜索被删除的请求包装和异常恢复入口，确认生产代码只剩一条 HTTP 请求恢复路径；最后同步现有执行文档。

---

## 13. 验收标准

1. `AgentLoop` 仍是父子 Agent 唯一执行循环；模型请求调用点已内联，HTTP Provider 异常只有 `tryRecover() → continue / 原异常抛出` 一条路径。
2. Manager 只按注册顺序分派 Handler；Content Handler 独立完成错误识别、次数限制和上下文修改，不调用模型或 Tool。
3. 可恢复的 Content Rejection 会真实发起下一次模型请求；整批 pending Tool Result 被原子替换，`tool_use`、tool input、`tool_use_id` 和工具已执行事实均保留。
4. 没有完整 pending 批次、次数耗尽、Prompt Too Long 或未知 Provider 错误时不重试、不修改历史，并原样抛出异常。
5. 模型请求被接受后通过新建 State 开启新的恢复链，后续恢复不继承上一条链的计数和 pending ID。
6. 主动上下文压缩、Tool Result 压缩、SSE/partial turn、CLI 回滚、TaskTool 协议和 `AgentResult` 类型体系保持现有行为。
7. `requestModel`、Prompt Too Long 异常恢复和 HTTP 旧恢复分支均无残留；没有新增外部依赖，也不存在新旧逻辑双轨运行。
8. 集成测试证明完整恢复链路，策略单元测试证明分派和原子修改边界；全部 Maven 测试及代码检查通过。
