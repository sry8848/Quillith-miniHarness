# manual-agent 工具审批架构重构执行文档

## 1. 本次改动范围

本次只重构工具调用执行前的审批架构，不实现交互模式、非交互模式或命令行参数解析。

目标是把工具审批从 Hook 体系中移出，形成独立的执行前审批层：

```text
完整 ToolCall
    ↓
ToolApprovalPolicy
    ↓
REQUIRED / NOT_REQUIRED
    ↓
ToolApprovalMode
    ├─ ASK：REQUIRED 时询问用户
    └─ BYPASS：不询问，直接放行
    ↓
ToolRegistry.execute()
    ↓
AgentTool.execute()
```

本次只实现以下审批规则：

| 工具            | 审批要求                                              |
| ------------- | ------------------------------------------------- |
| `write_file`  | Workspace 内 `NOT_REQUIRED`；Workspace 外 `REQUIRED` |
| `edit_file`   | Workspace 内 `NOT_REQUIRED`；Workspace 外 `REQUIRED` |
| `bash`        | `REQUIRED`                                        |
| 所有 `mcp__` 工具 | `REQUIRED`                                        |
| 其他工具          | `NOT_REQUIRED`                                    |

当前不实现：

- 系统 hard deny；
- Bash 命令黑名单；
- OS sandbox；
- per-tool policy registry；
- 通用规则引擎；
- Policy 动态加载；
- 新的交互或非交互入口；
- 工具执行后返回“需要审批”的结果。

审批和沙盒是两层独立能力。审批只决定工具执行前是否需要用户确认；它不限制进程、文件系统、网络或主机权限。

---

## 2. 结合现有代码确定的落点

### 2.1 审批入口放在 `AgentLoop.executeTool()`

当前所有合法、完整的工具调用最终都通过：

`src/main/java/dev/learn/agent/manual/AgentLoop.java`

中的：

```java
private ToolExecutionResult executeTool(ToolUseBlock toolUse)
```

进入统一执行管线。

前台工具和后台工具虽然由 `ToolCallDispatcher` 分流，但分流器收到的 action 仍然是：

```java
() -> executeTool(completedTool)
```

因此审批只需要接入 `AgentLoop.executeTool()`，不需要复制到：

- `ToolCallDispatcher`；
- `ToolExecutionScheduler`；
- `BackgroundTaskScheduler`；
- `ToolRegistry`；
- 每个具体 `AgentTool`。

### 2.2 审批检查放在前置 Hook 之后

当前前置 Hook 可以通过 `HookEffect.updatedInput()` 修改工具参数。审批 Policy 必须接收实际将执行的完整 `ToolCall`，不能审批原参数后再执行修改后的参数。

因此 `executeTool()` 内部顺序必须是：

```text
ToolCall.from(toolUse)
    ↓
HookRegistry.triggerBeforeToolUse(originalToolCall)
    ↓
如果 Hook BLOCK：返回失败，不询问审批
    ↓
应用 beforeEffect.updatedInput()，得到 effectiveToolCall
    ↓
ToolApprovalGate.approve(effectiveToolCall)
    ↓
如果用户拒绝：返回失败，不执行工具
    ↓
ToolRegistry.execute(effectiveToolCall)
    ↓
HookRegistry.triggerAfterToolUse(...)
```

这样既保留现有 Hook 扩展能力，又保证 Policy 判断的是最终执行参数。

### 2.3 Hook 不再承载审批

删除 `PermissionHook` 后，Hook 体系继续用于：

- 工具调用日志；
- 工作区日志；
- 大结果治理；
- Todo 提醒；
- Session Summary；
- 后台任务生命周期；
- 其他通用观测或扩展。

本次不删除 `AgentHook.beforeToolUse()`、`HookEffect.BLOCK` 或输入修改能力。这些是通用 Hook 契约，不等同于权限系统。只需保证工具审批不再通过 Hook 实现。

---

## 3. 新增独立审批包

在以下目录新增审批实现：

```text
src/main/java/dev/learn/agent/manual/tool/approval/
```

采用以下五个简单类型：

```text
ToolApprovalRequirement
ToolApprovalMode
ToolApprovalPolicy
DefaultToolApprovalPolicy
ToolApprovalGate
```

不增加 Policy Registry、Rule、Matcher、Sandbox 等额外抽象。

### 3.1 `ToolApprovalRequirement`

新增文件：

```text
src/main/java/dev/learn/agent/manual/tool/approval/ToolApprovalRequirement.java
```

职责：表达一次完整 `ToolCall` 是否需要审批。

建议内容：

```java
package dev.learn.agent.manual.tool.approval;

/**
 * 描述工具调用在执行前是否需要用户审批。
 */
public enum ToolApprovalRequirement {
    REQUIRED,
    NOT_REQUIRED
}
```

约束：

- 只能包含 `REQUIRED` 和 `NOT_REQUIRED`；
- 不增加 `DENIED`、`HARD_DENY` 或 `SANDBOX_REQUIRED`；
- 它是 Policy 的分类结果，不是工具执行结果；
- 用户拒绝不改变 ToolCall 的分类，拒绝只是本次审批结果。

### 3.2 `ToolApprovalMode`

新增文件：

```text
src/main/java/dev/learn/agent/manual/tool/approval/ToolApprovalMode.java
```

职责：控制如何处理 Policy 返回的 `REQUIRED`。

建议内容：

```java
package dev.learn.agent.manual.tool.approval;

/**
 * 控制需要审批的工具调用是否真正询问用户。
 */
public enum ToolApprovalMode {
    ASK,
    BYPASS
}
```

语义：

```text
ASK + REQUIRED        -> 询问用户
ASK + NOT_REQUIRED    -> 直接执行
BYPASS + REQUIRED     -> 直接执行
BYPASS + NOT_REQUIRED -> 直接执行
```

约束：

- Mode 不负责判断工具属于哪一类；
- Policy 不读取 Mode；
- `BYPASS` 只跳过人工询问，不把 Policy 的结果改成 `NOT_REQUIRED`。

### 3.3 `ToolApprovalPolicy`

新增文件：

```text
src/main/java/dev/learn/agent/manual/tool/approval/ToolApprovalPolicy.java
```

职责：接收完整 `ToolCall`，只返回审批要求。

建议接口：

```java
package dev.learn.agent.manual.tool.approval;

import dev.learn.agent.manual.tool.ToolCall;

/**
 * 在工具执行前判断本次调用是否需要审批。
 */
@FunctionalInterface
public interface ToolApprovalPolicy {

    /**
     * 判断完整工具调用的审批要求。
     *
     * @param toolCall 实际准备执行的完整工具调用
     * @return REQUIRED 或 NOT_REQUIRED
     */
    ToolApprovalRequirement requirementFor(
            ToolCall toolCall
    );
}
```

接口不得依赖：

- `Scanner`；
- `System.in`；
- `ToolApprovalMode`；
- `HookEffect`；
- `ToolExecutionResult`；
- `ToolRegistry`；
- 任何具体工具执行实例。

Policy 不询问用户、不执行工具、不返回拒绝原因。

### 3.4 `DefaultToolApprovalPolicy`

新增文件：

```text
src/main/java/dev/learn/agent/manual/tool/approval/DefaultToolApprovalPolicy.java
```

职责：集中实现当前所有工具审批规则。

字段只需要：

```java
private static final String MCP_TOOL_PREFIX = "mcp__";

private final WorkspacePathResolver paths;
```

构造方法只注入 `WorkspacePathResolver`：

```java
public DefaultToolApprovalPolicy(
        WorkspacePathResolver paths
)
```

`requirementFor()` 建议使用一个清晰的前缀判断和 `switch`：

```java
@Override
public ToolApprovalRequirement requirementFor(
        ToolCall toolCall
) {
    String toolName = toolCall.name();

    if (toolName.startsWith(MCP_TOOL_PREFIX)) {
        return ToolApprovalRequirement.REQUIRED;
    }

    return switch (toolName) {
        case "bash" ->
                ToolApprovalRequirement.REQUIRED;

        case "write_file", "edit_file" ->
                fileWriteRequirement(toolCall);

        case "read_file", "glob" ->
                ToolApprovalRequirement.NOT_REQUIRED;

        default ->
                ToolApprovalRequirement.NOT_REQUIRED;
    };
}
```

不要创建：

- 每个工具一个 Policy；
- 工具名到 Policy 的 Map；
- 可配置规则 DSL；
- 通用权限表达式；
- Bash 命令解析器；
- MCP 读写工具白名单。

### 3.5 文件写入分类

`DefaultToolApprovalPolicy` 新增私有方法：

```java
private ToolApprovalRequirement fileWriteRequirement(
        ToolCall toolCall
)
```

逻辑：

1. 从 `toolCall.input()` 读取 `path`；
2. `path` 不存在或不是字符串时返回 `NOT_REQUIRED`，让实际文件工具处理参数错误；
3. `write_file` 使用允许 Workspace 外路径的写入解析方法；
4. `edit_file` 使用允许 Workspace 外路径的已存在目标解析方法；
5. 解析结果位于 Workspace 内，返回 `NOT_REQUIRED`；
6. 解析结果位于 Workspace 外，返回 `REQUIRED`；
7. 路径解析失败时返回 `NOT_REQUIRED`，让实际工具返回真实参数或 IO 错误。

建议结构：

```java
private ToolApprovalRequirement fileWriteRequirement(
        ToolCall toolCall
) {
    JsonNode pathNode =
            toolCall.input().get("path");

    if (pathNode == null
            || !pathNode.isTextual()) {
        return ToolApprovalRequirement.NOT_REQUIRED;
    }

    try {
        Path target =
                switch (toolCall.name()) {
                    case "write_file" ->
                            paths.resolveForWriteAnywhere(
                                    pathNode.textValue()
                            );

                    case "edit_file" ->
                            paths.resolveExistingAnywhere(
                                    pathNode.textValue()
                            );

                    default ->
                            throw new IllegalStateException(
                                    "不支持的写入工具："
                                            + toolCall.name()
                            );
                };

        return paths.isInsideWorkspace(target)
                ? ToolApprovalRequirement.NOT_REQUIRED
                : ToolApprovalRequirement.REQUIRED;
    } catch (IOException
             | InvalidPathException exception) {
        return ToolApprovalRequirement.NOT_REQUIRED;
    }
}
```

这里返回 `NOT_REQUIRED` 不代表错误路径是安全路径。它表示 Policy 无法形成一个可执行的 Workspace 外写入目标，不负责把工具输入错误转成审批拒绝；实际工具随后会使用同一解析器并返回失败。

### 3.6 `ToolApprovalGate`

新增文件：

```text
src/main/java/dev/learn/agent/manual/tool/approval/ToolApprovalGate.java
```

职责：组合 Policy、Mode 和当前控制台询问流程，但不执行工具。

字段：

```java
private final ToolApprovalPolicy policy;
private final ToolApprovalMode mode;
private final Scanner scanner;
```

构造方法：

```java
public ToolApprovalGate(
        ToolApprovalPolicy policy,
        ToolApprovalMode mode,
        Scanner scanner
)
```

构造契约：

- `policy` 必须非空；
- `mode` 必须非空；
- `ASK` 模式要求 `scanner` 非空；
- `BYPASS` 模式允许 `scanner` 为空。

对外只需要一个方法：

```java
public boolean approve(ToolCall toolCall)
```

执行顺序必须先调用 Policy，再处理 Mode：

```java
public boolean approve(
        ToolCall toolCall
) {
    ToolApprovalRequirement requirement =
            policy.requirementFor(toolCall);

    if (requirement
            == ToolApprovalRequirement.NOT_REQUIRED) {
        return true;
    }

    if (mode == ToolApprovalMode.BYPASS) {
        return true;
    }

    return askUser(toolCall);
}
```

即使是 `BYPASS` 模式，也不要把 Mode 传给 Policy，也不要让 Policy 返回不同分类。

`askUser()` 从原 `PermissionHook` 平移过来，保持：

- 方法为 `synchronized`；
- 打印工具名和完整输入；
- 只有 `y` 或 `yes` 返回 `true`；
- 其他输入返回 `false`。

提示文本使用通用审批语义，不在 Gate 中重复工具分类规则：

```text
工具调用需要审批
工具：<tool name>
参数：<tool input>
是否允许执行？[y/N]
```

不要让 Gate 根据工具名生成 Bash、MCP 或 Workspace 专用规则；规则只属于 `DefaultToolApprovalPolicy`。

---

## 4. 删除 `PermissionHook`

删除文件：

```text
src/main/java/dev/learn/agent/manual/hook/hooks/PermissionHook.java
```

删除内容包括：

- `DENY_LIST`；
- `APPROVAL_REQUIRED` Bash 字符串列表；
- Bash 命令内容匹配；
- Git MCP 只读工具集合；
- Git/GitHub MCP 专用审批方法；
- Workspace 文件写入审批；
- Scanner 询问逻辑；
- 所有通过 `HookEffect.block()` 表达的权限拒绝。

其中需要保留的用户询问行为移动到 `ToolApprovalGate`，不复制两份。

同时删除：

- `ManualAgentApplication` 中的 `PermissionHook` import；
- `PermissionHook` 的构造代码；
- 父 HookRegistry 中的 `permissionHook` 注册；
- 子 HookRegistry 中的 `permissionHook` 注册；
- 所有描述“Hook 负责权限检查”的注释。

本次没有 `PermissionHook` 专项测试；如果后续发现旧测试引用该类，应删除旧测试并用 Policy/Gate 测试替代，不保留兼容包装类。

不要保留一个内部转发到新审批层的空 `PermissionHook`。审批已经离开 Hook 体系，旧类应直接删除。

---

## 5. 修改 `AgentLoop`

文件：

```text
src/main/java/dev/learn/agent/manual/AgentLoop.java
```

### 5.1 新增依赖

新增字段：

```java
private final ToolApprovalGate approvalGate;
```

字段放在 `ToolRegistry` 附近，因为两者都属于工具执行边界。

构造方法在 `ToolRegistry` 后增加参数：

```java
ToolRegistry toolRegistry,
ToolApprovalGate approvalGate,
HookRegistry hookRegistry,
```

构造方法中使用 `Objects.requireNonNull()` 保存，Javadoc 增加对应 `@param`。

不要把 `ToolApprovalPolicy`、`ToolApprovalMode` 和 `Scanner` 分别注入 `AgentLoop`。`AgentLoop` 只依赖已经组装好的 `ToolApprovalGate`，避免核心循环负责控制台细节。

### 5.2 调整 `executeTool()`

保留现有步骤：

1. `ToolCall.from(toolUse)`；
2. `triggerBeforeToolUse()`；
3. 前置 Hook BLOCK 处理；
4. 应用 `updatedInput`；
5. `ToolRegistry.execute()`；
6. `triggerAfterToolUse()`；
7. 合并 Hook 输出和附加上下文；
8. 统一异常处理。

只在第 4 步和第 5 步之间增加审批 Gate。

目标结构应等价于：

```java
HookEffect beforeEffect =
        hookRegistry.triggerBeforeToolUse(
                toolCall
        );

ToolExecutionResult executionResult;
HookEffect combinedEffect = beforeEffect;

if (beforeEffect.decision()
        == HookEffect.Decision.BLOCK) {
    executionResult =
            ToolExecutionResult.failure(
                    beforeEffect.reason()
            );
} else {
    ToolCall effectiveToolCall =
            beforeEffect.updatedInput() == null
                    ? toolCall
                    : toolCall.withInput(
                            beforeEffect.updatedInput()
                    );

    if (!approvalGate.approve(effectiveToolCall)) {
        executionResult =
                ToolExecutionResult.failure(
                        "Permission denied by user"
                );
    } else {
        executionResult =
                toolRegistry.execute(
                        effectiveToolCall
                );

        HookEffect afterEffect =
                hookRegistry.triggerAfterToolUse(
                        effectiveToolCall,
                        executionResult.content()
                );

        combinedEffect =
                beforeEffect.and(afterEffect);

        if (afterEffect.updatedOutput() != null) {
            executionResult =
                    new ToolExecutionResult(
                            afterEffect.updatedOutput(),
                            executionResult.error()
                    );
        }
    }
}
```

必须保证：

- Gate 收到 `effectiveToolCall`；
- 用户拒绝时不调用 `ToolRegistry.execute()`；
- 用户拒绝时不调用 `triggerAfterToolUse()`；
- 用户拒绝仍返回与原 `tool_use_id` 配对的失败 `tool_result`；
- `BYPASS` 模式仍走同一个 Gate，只是不询问；
- 工具执行后不得返回“需要审批，请重试”之类结果。

### 5.3 更新注释

更新 `executeTool()` 中以下旧表述：

- “Hook 和工具注册表共用”可以保留；
- “触发工具执行前 Hook，取得权限决策”改为“触发通用前置 Hook，取得扩展决策和输入修改”；
- “被权限拒绝的工具”改为“未实际执行的工具”；
- catch 注释中的“Hook、权限或工具管线”改为“Hook、审批或工具管线”。

更新 `ToolCallDispatcher` 类注释：

```text
本类只决定调度路径，具体 Hook、审批和工具执行仍由调用方提供的公共管线完成。
```

不要在 `ToolCallDispatcher` 内调用 Policy 或 Gate。

---

## 6. 修改应用装配

文件：

```text
src/main/java/dev/learn/agent/manual/ManualAgentApplication.java
```

### 6.1 当前默认模式

当前应用只有交互式输入循环，本次固定默认：

```java
private static final ToolApprovalMode DEFAULT_APPROVAL_MODE =
        ToolApprovalMode.ASK;
```

不要在本次解析 `args`，也不要新增 `exec` 子命令。

未来接入时只需要在应用装配阶段选择：

```text
交互模式默认 -> ASK
交互模式显式绕过审批 -> BYPASS
非交互模式默认 -> BYPASS
```

### 6.2 创建审批对象

在创建 `Scanner` 和 HookRegistry 的装配位置附近新增：

```java
ToolApprovalPolicy approvalPolicy =
        new DefaultToolApprovalPolicy(
                paths
        );

ToolApprovalGate approvalGate =
        new ToolApprovalGate(
                approvalPolicy,
                DEFAULT_APPROVAL_MODE,
                scanner
        );
```

当前父 Agent 和子 Agent：

- 共用同一个 Workspace；
- 共用同一个控制台 Scanner；
- 应属于同一次应用审批模式。

因此二者必须共用同一个 `ToolApprovalGate`。Gate 内的 `askUser()` 继续串行读取 Scanner，避免并发工具同时等待同一行输入。

### 6.3 HookRegistry 移除审批

父 HookRegistry 改为不再注册 `permissionHook`：

```java
hookRegistry.registerAll(
        new WorkspaceLoggingHook(cwd),
        toolLoggingHook,
        largeOutputHook,
        new TodoReminderHook(todoState),
        new SessionSummaryHook(),
        new BackgroundTaskHook(
                parentBackgroundScheduler
        )
);
```

子 HookRegistry 同样移除：

```java
subagentHookRegistry.registerAll(
        toolLoggingHook,
        largeOutputHook,
        new BackgroundTaskHook(
                subagentBackgroundScheduler
        )
);
```

更新相关注释，改成“共享工具日志、大输出处理和审批 Gate”，不要继续写“共享权限 Hook”。

### 6.4 AgentLoop 构造调用

创建父、子 `AgentLoop` 时，在 `ToolRegistry` 后传入同一个 `approvalGate`：

```java
new AgentLoop(
        ...,
        subagentToolRegistry,
        approvalGate,
        subagentHookRegistry,
        ...
)
```

```java
new AgentLoop(
        ...,
        toolRegistry,
        approvalGate,
        hookRegistry,
        ...
)
```

不要为父、子 Agent 分别复制 Policy 规则。

---

## 7. 调整 Workspace 路径解析

文件：

```text
src/main/java/dev/learn/agent/manual/utils/WorkspacePathResolver.java
```

### 7.1 保留现有内部边界方法

以下方法继续保持“目标必须位于 Workspace 内”的现有语义：

```java
public Path resolveExisting(String userPath)
public Path resolveForWrite(String userPath)
```

它们继续供内部持久化模块使用，例如：

- `TaskStore`；
- `MemoryRepository`；
- `ContextManager`。

不要把这些内部调用批量替换成开放路径方法。Agent 文件工具可以访问 Workspace 外，不代表内部 `.tasks`、`.memory` 或上下文产物可以脱离 Workspace。

### 7.2 新增开放路径解析方法

新增：

```java
public Path resolveExistingAnywhere(
        String userPath
) throws IOException

public Path resolveForWriteAnywhere(
        String userPath
) throws IOException

public boolean isInsideWorkspace(
        Path target
)
```

语义：

- `resolveExistingAnywhere`：以 Workspace 作为相对路径基准，但允许最终真实路径位于 Workspace 外；
- `resolveForWriteAnywhere`：保留现有“已存在目标解析符号链接、未存在目标解析最近已存在父目录”的能力，但不执行 Workspace hard deny；
- `isInsideWorkspace`：对已经解析、规范化的目标做 Workspace 内外分类。

### 7.3 复用现有解析算法

不要复制两套完整路径解析代码。将现有方法改为调用带边界开关的私有实现：

```java
public Path resolveExisting(
        String userPath
) throws IOException {
    return resolveExisting(
            userPath,
            true
    );
}

public Path resolveExistingAnywhere(
        String userPath
) throws IOException {
    return resolveExisting(
            userPath,
            false
    );
}
```

写入解析同理：

```java
public Path resolveForWrite(
        String userPath
) throws IOException {
    return resolveForWrite(
            userPath,
            true
    );
}

public Path resolveForWriteAnywhere(
        String userPath
) throws IOException {
    return resolveForWrite(
            userPath,
            false
    );
}
```

私有实现必须继续保留：

- `normalize()`；
- 已存在目标的 `toRealPath()`；
- 符号链接解析；
- 新写入目标向上寻找最近已存在父目录；
- 父目录 `toRealPath()`；
- 未解析后缀重新拼接；
- 原有 IO 异常语义。

只有 `enforceWorkspace == true` 时调用 `ensureInsideWorkspace()`。

`isInsideWorkspace()` 建议只比较已解析路径：

```java
public boolean isInsideWorkspace(
        Path target
) {
    Objects.requireNonNull(
            target,
            "目标路径不能为空"
    );

    return target.toAbsolutePath()
            .normalize()
            .startsWith(workspace);
}
```

类注释改为说明它同时负责：

- 以 Workspace 为相对路径基准；
- 路径规范化和符号链接处理；
- 为内部模块提供 Workspace 强边界方法；
- 为 Agent 文件工具提供可分类的开放路径方法。

不要在这里实现审批模式、用户询问或 sandbox。

---

## 8. 修改文件工具

### 8.1 `ReadFileTool`

文件：

```text
src/main/java/dev/learn/agent/manual/tool/tools/ReadFileTool.java
```

修改：

1. 将：
   
   ```java
   paths.resolveExisting(...)
   ```
   
   改为：
   
   ```java
   paths.resolveExistingAnywhere(...)
   ```

2. 更新类注释和 Tool description，说明 `path` 可以是相对于 Workspace 的路径，也可以是主机可访问的外部路径。

3. 保留 UTF-8 读取、普通文件检查、行数限制和错误返回。

4. 不向 `ReadFileTool` 注入 Policy、Mode 或 Gate。

`read_file` 的审批分类固定为 `NOT_REQUIRED`，但主机文件系统权限仍可能让实际读取失败。

### 8.2 `WriteFileTool`

文件：

```text
src/main/java/dev/learn/agent/manual/tool/tools/WriteFileTool.java
```

修改：

1. 将：
   
   ```java
   paths.resolveForWrite(...)
   ```
   
   改为：
   
   ```java
   paths.resolveForWriteAnywhere(...)
   ```

2. 更新类注释和 Tool description，说明允许 Workspace 外路径；是否询问由宿主审批层决定。

3. 保留参数校验、`Files.createDirectories()`、UTF-8 写入和 IO 错误返回。

4. 不在工具内部再次检查 Workspace 内外。

5. 不返回“需要审批”，因为进入工具时审批已经完成或被 BYPASS。

### 8.3 `EditFileTool`

文件：

```text
src/main/java/dev/learn/agent/manual/tool/tools/EditFileTool.java
```

修改：

1. 将：
   
   ```java
   paths.resolveExisting(...)
   ```
   
   改为：
   
   ```java
   paths.resolveExistingAnywhere(...)
   ```

2. 更新类注释和 Tool description，说明允许 Workspace 外路径；是否询问由宿主审批层决定。

3. 保留普通文件检查、`old_text` 校验、第一次精确替换和 IO 错误返回。

4. 不在工具内部实现审批。

### 8.4 `GlobTool`

不修改 `GlobTool` 的扫描范围。

它继续：

```java
Files.walk(paths.workspace())
```

`glob` 只扫描 Workspace 是工具能力边界，不是审批 hard deny。它不需要审批，也不需要扩展为任意目录扫描。

---

## 9. Bash 与 MCP

### 9.1 `BashTool`

`BashTool` 不增加任何审批代码。

保留：

- 前台和后台执行模式；
- Bash 进程创建；
- 初始工作目录；
- 超时；
- 退出码；
- 输出截断；
- 活动进程关闭。

更新 Tool description，将“in the workspace”改成“以 Workspace 作为初始工作目录”，并明确当前不是文件系统沙盒。Bash 可以访问绝对路径、切换目录、启动其他进程或访问网络。

不要恢复以下规则：

- `rm -rf /` hard deny；
- `sudo` hard deny；
- `rm ` 询问；
- `chmod 777` 询问；
- 任何命令字符串匹配。

Policy 对整个 `bash` 工具统一返回 `REQUIRED`。

### 9.2 MCP

Policy 对所有名称以 `mcp__` 开头的工具统一返回 `REQUIRED`。

包括：

- Git MCP 只读工具；
- Git MCP 写入工具；
- GitHub MCP 工具；
- 未来新增命名空间中的 MCP 工具。

本次不修改：

- `McpAgentTool`；
- `GitMcpClient`；
- `GitHubMcpClient`；
- `registerMcpTools()`；
- MCP 工具 Schema 或调用结果转换。

审批属于宿主执行前边界，MCP 客户端只负责真实远程调用。

---

## 10. 更新现有 AgentLoop 构造调用

搜索所有：

```text
new AgentLoop(
```

当前至少包括：

- `ManualAgentApplication` 中的父 Agent；
- `ManualAgentApplication` 中的子 Agent；
- `AgentLoopModelRequestRecoveryIntegrationTest` 中的测试 Agent。

生产代码传入共享 `approvalGate`。

测试代码不应依赖 stdin。对于与审批无关的 AgentLoop 测试，创建：

```java
ToolApprovalGate approvalGate =
        new ToolApprovalGate(
                new DefaultToolApprovalPolicy(paths),
                ToolApprovalMode.BYPASS,
                null
        );
```

再传入 `AgentLoop`。

如果测试当前内联创建 `WorkspacePathResolver`，先提取同一个局部变量供 `ContextManager` 和 `DefaultToolApprovalPolicy` 共用，不要创建语义不同的两个 Workspace 解析器。

---

## 11. 测试要求

### 11.1 `DefaultToolApprovalPolicyTest`

新增文件：

```text
src/test/java/dev/learn/agent/manual/tool/approval/DefaultToolApprovalPolicyTest.java
```

使用 `@TempDir` 创建 Workspace 和 Workspace 外目录。每个测试方法按项目约定增加 BDD 行为注释。

至少覆盖：

1. `read_file` 返回 `NOT_REQUIRED`；
2. `glob` 返回 `NOT_REQUIRED`；
3. Workspace 内 `write_file` 返回 `NOT_REQUIRED`；
4. Workspace 外 `write_file` 返回 `REQUIRED`；
5. Workspace 内 `edit_file` 返回 `NOT_REQUIRED`；
6. Workspace 外 `edit_file` 返回 `REQUIRED`；
7. `bash` 无论命令内容都返回 `REQUIRED`；
8. `mcp__git__git_status` 返回 `REQUIRED`，证明不再保留 Git 只读白名单；
9. `mcp__github__...` 返回 `REQUIRED`；
10. 任意未来 `mcp__other__...` 返回 `REQUIRED`；
11. `todo_write` 等其他工具返回 `NOT_REQUIRED`；
12. 文件工具缺少合法 `path` 时返回 `NOT_REQUIRED`，实际工具负责参数错误。

Policy 测试中不要创建 Scanner，不要测试 ASK/BYPASS，也不要执行真实 Tool。

### 11.2 `ToolApprovalGateTest`

新增文件：

```text
src/test/java/dev/learn/agent/manual/tool/approval/ToolApprovalGateTest.java
```

至少覆盖：

1. Policy 返回 `NOT_REQUIRED`，ASK 模式直接返回 `true`，不读取 Scanner；
2. Policy 返回 `REQUIRED`，ASK 模式输入 `y` 返回 `true`；
3. Policy 返回 `REQUIRED`，ASK 模式输入 `yes` 返回 `true`；
4. Policy 返回 `REQUIRED`，ASK 模式输入 `n` 返回 `false`；
5. Policy 返回 `REQUIRED`，BYPASS 模式、Scanner 为 `null` 时返回 `true`；
6. BYPASS 模式仍然调用一次 Policy，证明 Mode 没有绕开或改变分类职责；
7. ASK 模式构造时 Scanner 为 `null` 应尽早失败；
8. Policy 或 Mode 为 `null` 应尽早失败。

可以用 lambda 构造简单 Policy，不需要为测试新增假实现类。

### 11.3 `WorkspacePathResolverTest`

新增或补充：

```text
src/test/java/dev/learn/agent/manual/utils/WorkspacePathResolverTest.java
```

至少覆盖：

1. `resolveExisting()` 继续拒绝 Workspace 外已有路径；
2. `resolveForWrite()` 继续拒绝 Workspace 外写入路径；
3. `resolveExistingAnywhere()` 可以解析 Workspace 外已有路径；
4. `resolveForWriteAnywhere()` 可以解析 Workspace 外新路径；
5. `isInsideWorkspace()` 正确区分 Workspace 内外；
6. `..` 路径经过规范化后正确分类；
7. 可以稳定创建符号链接的环境中，Workspace 内指向外部的链接应按真实外部路径分类；如果 Windows 测试环境没有创建符号链接权限，不要写依赖该权限才能通过的测试。

### 11.4 文件工具测试

新增或补充文件工具测试，至少验证：

1. `ReadFileTool` 能读取 Workspace 外已有文件；
2. `WriteFileTool` 能写入 Workspace 外文件；
3. `EditFileTool` 能修改 Workspace 外已有文件；
4. 工具参数错误和 IO 错误仍返回 `ToolExecutionResult.failure()`；
5. 文件工具中不存在审批提示或 Mode 判断。

这些测试只验证 Tool 能力。是否应该执行已经由 Policy/Gate 测试覆盖，不要在工具测试中重复模拟审批。

### 11.5 验证命令

在：

```text
D:\learn_claudecode\learn-claude-code\demo\manual-agent
```

执行：

```powershell
mvn -q test
```

必须保证全部既有测试和新增测试通过。

---

## 12. 文件改动清单

### 12.1 删除

```text
src/main/java/dev/learn/agent/manual/hook/hooks/PermissionHook.java
```

### 12.2 新增

```text
src/main/java/dev/learn/agent/manual/tool/approval/ToolApprovalRequirement.java
src/main/java/dev/learn/agent/manual/tool/approval/ToolApprovalMode.java
src/main/java/dev/learn/agent/manual/tool/approval/ToolApprovalPolicy.java
src/main/java/dev/learn/agent/manual/tool/approval/DefaultToolApprovalPolicy.java
src/main/java/dev/learn/agent/manual/tool/approval/ToolApprovalGate.java
src/test/java/dev/learn/agent/manual/tool/approval/DefaultToolApprovalPolicyTest.java
src/test/java/dev/learn/agent/manual/tool/approval/ToolApprovalGateTest.java
src/test/java/dev/learn/agent/manual/utils/WorkspacePathResolverTest.java
```

文件工具测试可以按现有测试组织方式新增一个集中测试类，或分别放入三个 Tool 测试类，不为测试额外设计生产抽象。

### 12.3 修改

```text
src/main/java/dev/learn/agent/manual/AgentLoop.java
src/main/java/dev/learn/agent/manual/ManualAgentApplication.java
src/main/java/dev/learn/agent/manual/tool/ToolCallDispatcher.java
src/main/java/dev/learn/agent/manual/utils/WorkspacePathResolver.java
src/main/java/dev/learn/agent/manual/tool/tools/ReadFileTool.java
src/main/java/dev/learn/agent/manual/tool/tools/WriteFileTool.java
src/main/java/dev/learn/agent/manual/tool/tools/EditFileTool.java
src/main/java/dev/learn/agent/manual/tool/tools/BashTool.java
src/test/java/dev/learn/agent/manual/recovery/AgentLoopModelRequestRecoveryIntegrationTest.java
```

`ToolCallDispatcher` 和 `BashTool` 只更新与新边界不一致的注释或 Tool description，不改变调度和执行逻辑。

### 12.4 不修改业务逻辑

```text
src/main/java/dev/learn/agent/manual/hook/AgentHook.java
src/main/java/dev/learn/agent/manual/hook/HookEffect.java
src/main/java/dev/learn/agent/manual/hook/HookRegistry.java
src/main/java/dev/learn/agent/manual/tool/ToolRegistry.java
src/main/java/dev/learn/agent/manual/tool/ToolExecutionScheduler.java
src/main/java/dev/learn/agent/manual/background/BackgroundTaskScheduler.java
src/main/java/dev/learn/agent/manual/tool/tools/GlobTool.java
src/main/java/dev/learn/agent/manual/mcp/McpAgentTool.java
src/main/java/dev/learn/agent/manual/mcp/GitMcpClient.java
src/main/java/dev/learn/agent/manual/mcp/GitHubMcpClient.java
src/main/java/dev/learn/agent/manual/task/TaskStore.java
src/main/java/dev/learn/agent/manual/memory/MemoryRepository.java
src/main/java/dev/learn/agent/manual/context/ContextManager.java
```

---

## 13. 推荐实施顺序

1. 新增 `ToolApprovalRequirement`、`ToolApprovalMode` 和 `ToolApprovalPolicy`。
2. 修改 `WorkspacePathResolver`，新增开放解析和 Workspace 内外分类能力。
3. 新增 `DefaultToolApprovalPolicy` 并完成 Policy 单元测试。
4. 新增 `ToolApprovalGate` 并完成 Mode/询问单元测试。
5. 修改三个文件工具使用开放路径解析方法，并完成文件工具测试。
6. 修改 `AgentLoop`，在 effective ToolCall 与 `ToolRegistry.execute()` 之间接入 Gate。
7. 修改 `ManualAgentApplication`，创建共享 Policy/Gate，移除 PermissionHook 注册。
8. 删除 `PermissionHook.java`。
9. 更新所有 `AgentLoop` 构造调用和相关注释。
10. 更新 `BashTool`、`ToolCallDispatcher` 中不准确的权限或 sandbox 描述。
11. 执行 `mvn -q test`。
12. 检查 `git diff`，确认没有引入本任务之外的重构。

---

## 14. 完成验收标准

- [ ] `PermissionHook.java` 已删除。
- [ ] HookRegistry 不再注册任何权限 Hook。
- [ ] `ToolApprovalPolicy` 只接收完整 `ToolCall` 并返回 `REQUIRED` / `NOT_REQUIRED`。
- [ ] `DefaultToolApprovalPolicy` 不依赖 Scanner、Mode、Hook 或 ToolExecutionResult。
- [ ] `ToolApprovalMode` 只包含 `ASK` / `BYPASS`。
- [ ] `ToolApprovalGate` 负责 Mode 和用户询问，但不执行工具。
- [ ] `BYPASS` 不改变 Policy 分类，只跳过询问。
- [ ] 审批发生在 `ToolRegistry.execute()` 之前。
- [ ] Policy 检查的是应用前置 Hook 输入修改后的 effective ToolCall。
- [ ] 用户拒绝后不执行 Tool，也不触发 AfterToolUse Hook。
- [ ] `read_file` 和 `glob` 不需要审批。
- [ ] Workspace 内 `write_file`、`edit_file` 不需要审批。
- [ ] Workspace 外 `write_file`、`edit_file` 需要审批。
- [ ] 用户批准 Workspace 外写入后，文件工具能够真正尝试执行。
- [ ] 所有 Bash 调用需要审批。
- [ ] 所有 `mcp__` 工具需要审批。
- [ ] 不存在 Bash 命令黑名单和系统 hard deny。
- [ ] `WorkspacePathResolver` 保留规范化、真实路径和符号链接处理。
- [ ] 内部持久化模块继续使用 Workspace 强边界方法。
- [ ] 文件工具使用允许 Workspace 外目标的解析方法。
- [ ] 工具自身不返回“需要审批”。
- [ ] 前台、后台、父 Agent、子 Agent 都走同一个审批 Gate。
- [ ] 当前应用默认 `ASK`，但未提前实现 CLI 或非交互入口。
- [ ] 代码和注释明确说明审批不是 OS sandbox。
- [ ] `mvn -q test` 全部通过。
